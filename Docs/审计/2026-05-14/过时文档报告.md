# 过时文档报告

生成时间: 2026-05-14 06:10:04 +08:00
审计原则: 以当前源码和文件存在性为准，历史审计报告只作为背景，不作为事实来源。
审计范围: `Docs/`、根目录入口文档、内容池/推进/伤害/UI 指南。

## 总体结论

文档健康度为 **中**。上轮修正过的旧资源路径问题大多没有继续出现在主文档中，但当前仍存在两类风险：

1. 文档把“目标架构”写成“当前实现”，尤其是内容池历史状态。
2. 部分文档仍引用已不存在或已改名的运行时类。

项目根目录没有当前项目 `README.md`，新人缺少可信入口和场景装配说明。

## 失效或冲突文档清单

| 问题等级 | 优先级 | 文件路径 | 位置 / 内容 | 问题描述 / 与代码冲突点 | 风险分析 | 修复建议 |
|---|---|---|---|---|---|---|
| 高 | P0 | `Docs/统一内容池系统使用指南.md` | 第 96-108 行，`ContentHistoryState` 说明 | 文档写明升级卡、商店、宝箱、掉落、刷怪和武器奖励都应写入内容系统统一历史，不再由模块维护私有历史。当前代码中 `ShopManager` 和 `RewardSelectionManager` 各自 `new ContentHistoryState()`，`AddRandomWeaponCard` 使用静态 `weaponRewardHistoryState`。 | 新业务按文档理解会以为存在统一 per-run 历史源，但实际历史按模块和部分静态状态分散，`MaxPickCount`、上次 offer、标签选择次数可能跨业务不共享。 | 明确当前实现是“按业务域分散历史”还是“准备统一历史”。若要统一，新增 per-run `ContentHistoryState` 拥有者并由 Bootstrap 注入；若不统一，改文档措辞。 |
| 中高 | P1 | `Docs/统一内容池系统使用指南.md` | 第 263 行，刷怪入口 | 文档写 `WaveSpawnExecutionService`，当前代码实际是 `Assets/Scripts/Waves/Services/WaveSpawnExecutor.cs`，由 `WaveManager.ProcessCurrentWaveSpawns` 调用。 | 维护者按文档搜索会找不到类，排查刷怪权重、标签和推进倍率时会走错入口。 | 将文档入口改为 `WaveSpawnExecutor`，并补 `WaveManager -> WaveSpawnExecutor -> EnemyFactory` 当前链路。 |
| 中高 | P1 | `Docs/统一内容池系统使用指南.md` | 第 282-284、387 行示例 | 示例使用 `ContentPoolPurpose.Shop`，当前代码使用 `ContentPoolScopeIds.Shop` / 字符串 scope。 | 按示例写新代码会编译失败，或者误以为仍有 `ContentPoolPurpose` 枚举模型。 | 把示例改成 `new ContentHistoryScope(ContentPoolScopeIds.Shop, pool.name, ownerId)` 和 `new ContentRollContext(ContentPoolScopeIds.Shop, ...)`。 |
| 中高 | P1 | `Docs/局内推进系统设计与使用说明.md` | 第 50、68、191 行 | 文档多处引用 `WaveSpawnExecutionService`；当前实现为 `WaveSpawnExecutor`，并由 `WaveManager` 在 `Update` 中执行。 | 推进倍率与刷怪关系是核心数值链路，入口名称错误会影响调参与问题定位。 | 替换为 `WaveSpawnExecutor`，并更新流程图中的类名和文件路径。 |
| 中 | P1 | `Docs/伤害结算流程说明.md` | 第 187-193 行近战武器调用链 | 文档写 `MeleeWeaponAttackExecutor` 命中目标；当前源码未找到该类，近战命中实际由 `Weapon.cs` 内部 `HitBoxAttackExecutor.ExecuteAttack` 采样后调用 `HitService.Apply`。 | 排查近战命中或想扩展近战窗口时，文档会把维护者带向不存在的类。 | 改为 `Weapon.OpenHitWindow/TickActiveHitWindows -> HitBoxAttackExecutor.ExecuteAttack -> HitService.Apply`。 |
| 中 | P2 | 根目录 | 缺少项目级 `README.md` | `rg --files -g README*` 只找到 DOTween 自带 readme，项目根没有可信入口文档。 | 新人不知道 Unity 版本、启动场景、必备资源、场景装配、测试命令和文档入口。 | 新增根 `README.md`，包含 Unity 版本、入口场景、包依赖、关键目录、必读文档和测试/构建方式。 |
| 中 | P2 | `Docs/审计/2026-05-12/*.md`, `Docs/审计/2026-05-13/*.md` | 历史审计快照 | 旧报告仍提到 `MonoSingletonBase`、`GameInputService`、`ContentPoolRuntimeState`、`UpgradeRunState` 等历史状态；当前主源码中这些已不应当被直接当作当前事实。 | 历史报告有保留价值，但如果被当作最新健康结论，会误导修复优先级。 | 保留归档，不改写；在最新报告中明确“历史审计不是当前实现说明”。 |
| 低 | P3 | `Docs/OrangeUIFramework通用指南.md` | 第 82-111 行 | 文档标题说是通用框架指南，但包含 Survivors 当前项目的 `Assets/GameContent/UI/` 资产落地路径。内容本身基本准确，但通用框架文档和项目落地指南混在一起。 | 框架迁移到其他项目时，这段会成为项目特化噪音。 | 拆成 `OrangeUIFramework通用指南.md` 与 `Survivors UI资源落地指南.md`。 |

## 缺失文档

| 等级 | 优先级 | 缺失文档 | 影响范围 | 建议内容 |
|---|---|---|---|---|
| 高 | P0 | 场景装配契约 | `GameManager`, `UIManager`, `GameContentBootstrap`, `GameInputSceneBinder`, `WaveManager`, `EnemyRegistry`, `Player` | 列出 Game Scene 必备对象、必填 Inspector 字段、允许自动查找的引用、缺失时的失败行为。 |
| 中高 | P1 | 内容历史归属说明 | 商店、升级奖励、宝箱、掉落、刷怪、随机武器奖励 | 明确每个业务池的 `ContentHistoryState` 拥有者、生命周期和共享/隔离规则。 |
| 中 | P2 | 输入模块装配说明 | `GameInput`, `InputModuleRuntime`, `InputModuleProfile`, `GameInputSceneBinder`, 设置面板 | 说明重绑存储、默认 Context、UI EventSystem 绑定和跨场景生命周期。 |
| 中 | P2 | 测试入口说明 | `Assets/Tests`, `Assets/Scripts/*/Tests` | 列出可运行的 EditMode/PlayMode 测试、推荐命令、常见失败处理。 |

## 文档修复优先级

1. P0: 先修 `Docs/统一内容池系统使用指南.md` 的 `ContentHistoryState` 归属描述。
2. P1: 修正 `WaveSpawnExecutionService` 和 `MeleeWeaponAttackExecutor` 这类不存在的类名。
3. P1: 补场景装配契约，防止隐式依赖继续扩散。
4. P2: 补项目根 `README.md`。
