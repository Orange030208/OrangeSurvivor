# 过时文档报告

生成时间: 2026-05-15 06:05:13 +08:00

审计范围: `Docs` 当前主文档、项目根文档入口、文档与 `Assets/Scripts` 实现的一致性。`Docs/审计/*` 视为历史快照，不作为当前实现说明。

## 总体结论

文档风险等级为中高。路径漂移相比 2026-05-12 已明显减少，但仍存在三类会误导实现的文档腐化:

1. 文档使用了当前源码中不存在的类名。
2. 文档仍使用旧术语 `ContentPoolPurpose`，当前源码实际使用 `ContentPoolScopeIds` 字符串作用域。
3. `ContentHistoryState` 被描述为内容系统统一维护的运行时历史，但当前代码由多个业务对象分别持有历史。

项目根目录仍未发现项目级 `README.md`，新人缺少可信入口。

## 已确认过时或冲突的文档

| 严重等级 | 优先级 | 文件路径 | 问题位置 | 问题描述 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 高 | P0 | `Docs/统一内容池系统使用指南.md` | 第 96-108 行，`ContentHistoryState` 说明 | 文档写明 `ContentHistoryState` 是内容系统统一维护的运行时历史，并要求升级卡、商店、宝箱、掉落、刷怪和武器奖励都不再维护私有抽取历史。当前代码实际为多个持有者: `ShopManager.contentHistoryState`、`RewardSelectionManager.contentHistoryState`、`DropManager.contentHistoryState`、`WaveSpawnExecutor.contentHistoryState`、`AddRandomWeaponCard.weaponRewardHistoryState`。 | 新业务按文档理解会以为存在统一 per-run 历史源，但实际 pick/roll 历史按业务对象隔离。`MaxPickCount`、上一轮 offer、标签选择次数是否共享会被误判。 | 先决定真实策略。若统一，新增运行局级 `RunContentHistory` 并注入各业务；若不统一，文档改为“按业务池持有历史”，列出每个持有者、作用域和生命周期。 |
| 高 | P0 | `Docs/统一内容池系统使用指南.md` | 第 282-287、387 行 | 文档示例和维护约束仍使用 `ContentPoolPurpose.Shop` / `ContentPoolPurpose`。当前源码未发现 `ContentPoolPurpose`，实际为 `ContentPoolScopeIds.Shop` 等字符串常量。 | 维护者复制示例会编译失败，或在新增业务池时找不到枚举扩展点。 | 将示例改为 `ContentPoolScopeIds.Shop`；维护约束改为“新增业务池先扩展 `ContentPoolScopeIds` 或明确新的 scopeId 常量”。 |
| 中高 | P1 | `Docs/统一内容池系统使用指南.md` | 第 257-265 行，刷怪池入口 | 文档写运行时入口为 `WaveSpawnExecutionService`。当前源码实际是 `Assets/Scripts/Waves/Services/WaveSpawnExecutor.cs`，由 `WaveManager.ProcessCurrentWaveSpawns` 调用。 | 排查刷怪权重、标签、推进倍率时会搜索不到入口，浪费定位时间。 | 改为 `WaveManager -> WaveSpawnExecutor -> EnemyFactory`，并写清 `WaveSpawnExecutor` 自身也持有 `ContentHistoryState`。 |
| 中高 | P1 | `Docs/局内推进系统设计与使用说明.md` | 第 50、68、191 行 | 多处引用 `WaveSpawnExecutionService`，当前实现为 `WaveSpawnExecutor`。 | 局内推进与刷怪倍率是核心数值链路，入口名错误会影响调参与问题定位。 | 替换类名和文件路径，更新流程描述为 `WaveManager.Update` 驱动 `WaveSpawnExecutor.Execute`。 |
| 中 | P1 | `Docs/伤害结算流程说明.md` | 第 190 行 | 文档写 `MeleeWeaponAttackExecutor` 命中目标。当前源码未找到该类，近战命中实际由 `Weapon.OpenHitWindow/TickActiveHitWindows -> HitBoxAttackExecutor.ExecuteAttack -> HitService.Apply` 完成。 | 扩展近战命中窗口或排查伤害时会被带到不存在的类。 | 更新调用链和流程图，明确 `HitBoxAttackExecutor` 是当前近战采样执行者。 |
| 中 | P1 | `Docs/数值系统落地规范.md` | 第 36 行 | 文档写商店从 `ContentPoolPurpose.Shop` 内容池抽取商品。当前实现是 `ShopManager.CreateShopRollContext` 使用 `ContentPoolScopeIds.Shop`。 | 数值文档面向策划和程序协作，旧术语会导致配置沟通不一致。 | 替换为 `ContentPoolScopeIds.Shop`，并说明价格仍由 `ShopPricingService` 计算。 |
| 中 | P2 | 项目根目录 | 缺少 `README.md` | `rg --files -g README*` 在排除第三方目录后无结果。 | 新人不知道 Unity 版本、入口场景、必备包、核心目录、测试方式和文档状态。 | 新增根 `README.md`，包含 Unity 2022.3.62f3c1、入口场景、核心目录、常用测试命令、必读文档和文档状态说明。 |
| 低 | P3 | `Docs/OrangeUIFramework通用指南.md` | 第 85、100、111 行 | 文档标题是通用框架指南，但包含当前工程 `Assets/GameContent/UI/` 落地路径。内容本身有标注“当前工程约定”，不算错误，但通用框架和项目落地仍混在同一文档。 | 框架迁移到其他项目时，这段会成为项目特化噪音。 | 拆出 `Survivors UI资源落地指南.md`，框架指南只保留 API 和可迁移约束。 |
| 低 | P3 | `Docs/命中事务流水线设计方案.md` | 第 3 行 | 文档明确写明是目标架构，不是当前实现说明。 | 本身未失效，但容易在搜索中与当前伤害流程文档混淆。 | 保留，但建议移入 `Docs/设计/` 或文件名加 `目标方案`，首屏继续保留状态标记。 |

## 文档缺失

| 严重等级 | 优先级 | 缺失文档 | 影响范围 | 建议内容 |
| --- | --- | --- | --- | --- |
| 中 | P2 | 根 `README.md` | 新人接手、自动化、CI、测试入口 | Unity 版本、推荐打开场景、包依赖、目录结构、运行/测试命令、文档地图。 |
| 中高 | P1 | 内容历史生命周期说明 | 内容池、商店、掉落、刷怪、奖励 | 明确 `ContentHistoryState` 当前持有者、scopeId、poolId、ownerId、reset 时机、是否跨业务共享。 |
| 中 | P2 | 场景装配契约 | `GameManager`, `WaveManager`, `UIManager`, `GameContentBootstrap`, `GameInput` | 每个核心场景必须挂载的对象、序列化引用、可选引用、启动顺序、缺失时错误。 |
| 中 | P2 | 测试入口说明 | `Assets/Tests/Editor`, `Assets/Scripts/*/Tests` | 可运行测试集、推荐命令、常见 warning/失败处理。 |

## 历史文档处理建议

`Docs/审计/2026-05-12/*`、`Docs/审计/2026-05-13/*`、`Docs/审计/2026-05-14/*` 是历史快照，保留价值高，但不应作为当前实现说明。建议在 `Docs/审计/README.md` 或根 README 中标注: “审计报告是日期快照，以最新日期为准，旧报告中的类名和问题可能已被修复。”

## 优先修复顺序

1. P0: 修正 `Docs/统一内容池系统使用指南.md` 的 `ContentHistoryState` 归属和 `ContentPoolPurpose` 旧术语。
2. P1: 修正 `WaveSpawnExecutionService`、`MeleeWeaponAttackExecutor` 这类不存在类名。
3. P1: 同步 `Docs/数值系统落地规范.md` 的内容池术语。
4. P2: 新增项目根 `README.md` 和场景装配契约。
5. P3: 将目标方案文档与当前实现说明分区存放。
