# 过时文档报告

生成时间: 2026-05-13 06:09:11 +08:00

审计范围: `Docs`、根目录 Markdown、`Assets/GameContent`、`Assets/Scripts`

审计原则: 不默认文档可信，所有结论以当前代码和资产引用为准。

## 总体结论

昨天已修正的 `Assets/ScriptableObjects/...`、`Assets/Prefabs/UI/...` 等路径漂移在当前主要指南中没有继续命中。当前最需要处理的是“迁移期/目标方案文档”和“当前实现说明”混在一起，尤其是内容抽取系统。项目根目录没有 `README.md`，新人缺少一份可信入口文档。

## 失效或易误导文档

| 等级 | 文件路径 | 问题位置 | 问题描述 | 风险分析 | 修复建议 | 优先级 |
|---|---|---|---|---|---|---|
| 高 | `Docs/内容抽取系统重构方案.md` | 第 1-21、129-130、428-435、503-515 行 | 文档写明要移除 `FactDefinitionSO`、`ContentPoolRuntimeState`、`UpgradeRunState`，并且迁移完成后删除本文档。但当前代码仍存在这些类型和大量资产引用，同时新增了 `ContentHistoryState`。 | 该文档现在既像设计目标，又像未完成迁移清单。新成员无法判断当前系统到底应该用旧 Fact 链路还是新历史链路。 | 立即改成“内容抽取迁移状态报告”：列出已完成、未完成、保留原因和删除条件；或移入 `Docs/历史/` 并标注非当前实现。 | P0 |
| 中高 | `Docs/统一内容池系统使用指南.md` | 第 246-258、813-819 行 | 仍把 `ContentPoolRuntimeState` 作为主要运行时状态，并举例“升级卡系统保留 `UpgradeRunState.RecordPick`”；但当前 `UpgradeCardRollService.RecordPick` 是迁移期空方法，升级奖励流程已直接用 `ContentHistoryState.RecordPick`。 | 按文档实现新业务会把 pick 历史写到错误位置，导致 `MaxPickCount`、上一轮 offer、升级标签统计行为不一致。 | 同步当前混合状态：升级卡使用 `ContentHistoryState`，商店/掉落/刷怪仍使用 `ContentPoolRuntimeState`；明确后续统一方向。 | P0 |
| 中 | `Docs/伤害结算流程说明.md` | 第 190 行附近 | 文档仍提到 `MeleeWeaponAttackExecutor` 命中目标；当前代码未找到该类，武器近战命中实际在 `Weapon.cs` 的 `HitBoxAttackExecutor` 和 `HitService` 链路中完成。 | 维护者按旧名称搜索会找不到实现，排查近战命中问题时会浪费时间。 | 把流程图和示例类名改为 `Weapon` / `HitBoxAttackExecutor` / `HitService.Apply`。 | P1 |
| 中 | `Docs/ROGUELIKE_ITEM_INFO_DISPLAY_SYSTEM.md` | 第 631 行附近 | 文档建议新增 `StatIconCatalog`，但当前代码已有 `PropPresentationCatalogSO`、`GameContentRuntime.GetPropIcon` 等属性展示入口。 | 容易诱导新增第二套属性图标目录，造成 UI 表现配置分裂。 | 标注该文档为“方案草案”，或更新为当前 `PropPresentationCatalogSO` 方案。 | P2 |
| 中 | `Docs/命中事务流水线设计方案.md` | 第 3 行 | 文档明确写着“不是当前实现说明，而是后续重构目标”。 | 文档本身并未声称当前实现，但文件位于 Docs 根层，和当前说明混放，容易被误引用。 | 移入 `Docs/设计方案/` 或标题加“目标方案/非当前实现”。 | P2 |
| 低 | `Docs/审计/2026-05-12/*.md` | 多处 | 旧审计报告包含 `MonoSingletonBase`、`GameInputService`、旧路径等已变化内容。 | 审计报告是历史快照，本身不应改写；但如果被当作当前状态会误导。 | 保留归档，但在最新报告中明确“不要把旧审计当当前实现”。 | P3 |

## 缺失文档

| 等级 | 缺失内容 | 影响范围 | 风险分析 | 修复建议 | 优先级 |
|---|---|---|---|---|---|
| 高 | 根目录 `README.md` | 新人接手、自动化任务、基本验证流程 | 当前只有 `AGENTS.md` 和专题文档，没有统一入口说明 Unity 版本、启动场景、核心目录、测试命令、常见故障。 | 新增根 `README.md`：项目版本、依赖包、启动场景、运行/测试命令、目录索引、文档导航。 | P0 |
| 高 | 场景装配契约文档 | `GameContentBootstrap`、`GameManager`、`UIManager`、`WaveManager`、`GameInput`、`CharacterSelectionManager` | 运行时代码越来越依赖显式场景对象，但目前缺少一页说明“主场景必须放哪些对象、字段必须绑定哪些资产”。 | 新增 `Docs/场景装配契约.md`，列出必需对象、组件、引用、失败表现和验证方式。 | P0 |
| 中高 | 内容抽取系统当前架构图 | 升级、商店、掉落、刷怪 | 当前代码处于 `ContentHistoryState` 与 `ContentPoolRuntimeState` 并存阶段，缺少可信数据流图。 | 新增或重写内容池文档中的“当前状态”章节，明确每个业务池的历史状态归属。 | P1 |
| 中 | 程序集/模块边界说明 | 全部 `Assets/Scripts` | 当前没有 asmdef，目录边界无法约束依赖，新人只能靠搜索理解模块关系。 | 在拆 asmdef 前先写 `Docs/架构/模块边界草案.md`，作为后续迁移依据。 | P1 |
| 中 | UI 框架当前接入清单 | Orange UIFramework、业务 UI | UI 文档很完整，但缺少“当前项目实际注册了哪些 Page/Popup/Modal、Prefab 在哪”的索引。 | 从 `OrangeUIViewCatalog.asset` 生成或手写一份当前 UI Catalog 索引。 | P2 |

## 当前仍有效的文档信号

| 文件路径 | 当前判断 |
|---|---|
| `Docs/OrangeUIFramework通用指南.md` | 路径已基本对齐 `Assets/GameContent/UI/...`，并且明确 `warmupCount` 等能力以当前实现为准。 |
| `Docs/局内推进系统设计与使用说明.md` | 与 `WaveManager`、`RunProgressionRuntime`、`ShopManager` 当前数据流大体一致。 |
| `Docs/玩法数值规范.md` | 仍适合作为玩法数值改动前置阅读材料，但需要在 README 中建立入口。 |

## 建议修复顺序

1. 先处理 `Docs/内容抽取系统重构方案.md` 与 `Docs/统一内容池系统使用指南.md` 的冲突。
2. 新增根 `README.md` 和 `Docs/场景装配契约.md`。
3. 修正 `Docs/伤害结算流程说明.md` 中旧类名。
4. 把目标方案、历史方案和当前实现说明分目录管理。
