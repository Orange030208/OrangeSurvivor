# 重构路线图

生成时间: 2026-05-18 06:10:15 +08:00

目标: 先降低低成本高风险问题，再拆核心大类。当前不建议一次性大重构。

## 优先修复顺序

| 顺序 | 等级 | 优先级 | 目标 | 文件路径 | 当前问题 | 修复方案 | 验收标准 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 高 | P0 | 给事件总线增加自动 reset | `Assets/Scripts/Event/GameEventBus.cs`、`Assets/Tests/Editor/*` | 静态监听字典只靠手动 `Clear()`。 | 增加 SubsystemRegistration reset；补测试覆盖 reset 后监听计数为 0。 | EditMode 测试通过；重复进场景不出现重复事件。 |
| 2 | 高 | P0 | 给 JSON 导入删除行为加保护 | `Assets/Scripts/Editor/DataImport/BuffJsonAssetSync.cs`、`UpgradeCardJsonAssetSync.cs`、`WaveSpawnPackJsonAssetSync.cs`、`DataImportReport.cs` | Apply 会直接删除 JSON 中不存在的 Buff、升级卡、刷怪包资产。 | 默认 Apply 只创建/更新；删除走单独菜单或确认弹窗；报告写入磁盘。 | Preview 有删除候选时 Apply 不删除；确认删除有单独日志和可审查 diff。 |
| 3 | 高 | P0 | 修正文档旧术语和不存在类名 | `Docs/统一内容池系统使用指南.md`、`Docs/局内推进系统设计与使用说明.md`、`Docs/伤害结算流程说明.md`、`Docs/数值系统落地规范.md` | 主文档仍引用 `ContentPoolPurpose`、`WaveSpawnExecutionService`、`MeleeWeaponAttackExecutor`、`AddRandomWeaponCard`。 | 替换为 `ContentPoolScopeIds`、`WaveManager -> WaveSpawnExecutor -> EnemyFactory`、`Weapon -> HitBoxAttackExecutor -> HitService`、`RewardSelectionManager -> EquipmentRewardSelectionHandler.CreateWeapon`。 | `rg "ContentPoolPurpose|WaveSpawnExecutionService|MeleeWeaponAttackExecutor|AddRandomWeaponCard" Docs -g "*.md" -g "!Docs/审计/**"` 无命中。 |
| 4 | 中高 | P1 | 补 JSON 导入工具说明和导入报告落盘 | `Assets/Scripts/Editor/DataImport/*ImportMenu.cs`、`Docs/JSON数据导入工具使用说明.md` | 菜单只把报告打到 Console，导入者难以长期追踪。 | 每次 Preview/Apply 写入 `Docs/审计/<timestamp>/数值资产同步报告.md` 或 `Logs/DataImport`，文档说明回退流程。 | 每次导入都有可追溯报告；删除候选、阻塞项、警告可审查。 |
| 5 | 中高 | P1 | 定版内容历史生命周期 | `ContentHistoryState.cs`、`ShopManager.cs`、`RewardSelectionManager.cs`、`DropManager.cs`、`WaveSpawnExecutor.cs` | 多业务各自持有历史，文档说统一。 | 推荐新增 per-run `RunContentHistory`，业务按 scope 隔离。若维持分散，写入文档并补测试。 | 文档列出每个业务池历史持有者和 reset 时机；测试覆盖商店/奖励/掉落/刷怪历史共享或隔离。 |
| 6 | 中高 | P1 | 显式化波次位置策略迁移 | `WaveDefinitionSO.cs`、`SpawnLocationDefinition.cs`、`WaveJsonAssetSync.cs`、`Docs/波次刷怪位置策略说明.md` | 旧 `SpawnLocationPolicySO` 已删除，位置策略改为 `WaveDefinitionSO.spawnLocation` 内联，但文档和迁移校验不足。 | 写迁移说明；加 Editor 验证工具扫描所有 `WaveDefinitionSO` 的 resolverSettings 与 strategy。 | 所有波次资产都有有效 strategy；旧策略资产引用不再残留。 |
| 7 | 中高 | P1 | 拆分 `Weapon` 外围职责 | `Assets/Scripts/Weapons/Weapon.cs` | 武器门面仍承担攻击周期、命中窗口、投射物、序列事件、SFX/VFX、Gizmo。 | 第一阶段只拆 `WeaponGizmoDrawer`、`WeaponSequenceEventDispatcher`；第二阶段拆攻击周期和命中窗口。 | `WeaponRuntimeRefactorTests`、`WeaponMeleeStabilizationTests`、`PropertyMappingAndAttackUsageTests` 通过。 |
| 8 | 中高 | P1 | 抽出 `GameManager` 状态机 | `Assets/Scripts/Managers/GameManager.cs` | 状态迁移、UI、波末、暂停、音乐、玩家生成混在 MonoBehaviour。 | 新增纯 C# `GameStateMachine` 表达合法迁移和阻断规则；`GameManager` 保留 Unity 适配。 | 状态迁移有单元测试；公开行为不变。 |
| 9 | 中高 | P1 | 拆分奖励选择流程 | `Assets/Scripts/Managers/RewardSelectionManager.cs` | 队列、异步弹窗、暂停、内容池和选择应用混在一个类。 | 抽 `RewardSelectionFlow`、`RewardPauseScope`、`RewardPopupPresenter`。 | 连续升级/宝箱/状态切换测试通过，暂停释放成对。 |
| 10 | 中 | P2 | 统一 DOT 伤害结算口径 | `DamageOverTimeFeature.cs`、`HitService.cs`、`Docs/伤害结算流程说明.md` | DOT 直接 `ApplyHitResult`，武器/投射物走 `HitService`。 | 明确 DOT 是否受命中修饰。推荐改为 `HitService.Apply`，特殊规则通过 `HitSourceKind.Buff` 控制。 | DOT 测试覆盖伤害、死亡事件、是否受 modifier 影响。 |
| 11 | 中 | P2 | 拆分 `UIManager` 外围职责 | `Assets/Scripts/OrangeUIFramework/Core/Runtime/UIManager.cs` | 单类 1600+ 行，承担 UI 框架大部分能力。 | 先抽 `UIRootBuilder`、`UIBlockerController`、`UIDiagnosticsBuilder`，再拆 pool/registry。 | 公开 API 不变；UIFramework 设置测试通过；诊断输出一致。 |
| 12 | 中 | P2 | 显式化场景依赖 | `GameManager`、`ShopManager`、`RewardSelectionManager`、`StageCompleteSummaryManager`、`CameraController`、`GameContentRuntime` | 多处 `FindFirstObjectByType` 兜底。 | 新增场景装配契约；用 Bootstrap/context 注入玩家、钱包、UI、波次和内容 Provider。 | 主流程热路径不再查场景；漏配在启动时明确报错。 |
| 13 | 中 | P2 | 分阶段引入 asmdef | `Assets/Scripts`、`Assets/Tests/Editor` | 目录模块无编译边界。 | 先拆纯逻辑模块，再拆 UI/Waves/Managers。 | 无循环依赖；测试 asmdef 能引用目标模块；Assembly-CSharp 变薄。 |

## 最推荐先做

先执行 **P0-1 GameEventBus reset** 和 **P0-2 JSON 导入删除保护**。

原因:

- `GameEventBus` reset 是小改动高收益，可直接降低全局状态污染。
- JSON 导入删除保护与当前工作区资产删除风险直接相关，继续放任会扩大内容资产风险。
- 文档修复紧随其后，避免旧术语继续误导后续实现。

## 分阶段建议

### 阶段 1: 生命周期与资产安全

- `GameEventBus` 增加 SubsystemRegistration reset。
- `SpawnPositionResolver` 静态告警状态增加 reset 或移入实例。
- JSON 导入工具默认禁止删除 stale 资产。
- 补两类 EditMode 测试: 事件 reset、导入 Preview/Apply 删除保护。

### 阶段 2: 文档恢复可信

- 修正文档旧类名和旧术语。
- 新增 JSON 导入工具说明。
- 新增波次刷怪位置策略说明。
- 新增内容历史生命周期表。

### 阶段 3: 内容历史和结算口径定版

- 决定 ContentHistory 的 per-run / per-domain 策略。
- 推荐引入 `RunContentHistory`。
- 明确 DOT 是否走 `HitService`。
- 给商店、升级奖励、宝箱、武器奖励、掉落、刷怪、DOT 补行为测试。

### 阶段 4: 核心大类小步拆分

- `Weapon`: 先拆 Gizmo 和序列事件，再拆攻击周期。
- `GameManager`: 先抽状态机，再拆波末流程。
- `UIManager`: 先拆 Root/Layer/Diagnostics。
- `RewardSelectionManager`: 先抽暂停 scope，再抽队列 flow。

## 风险控制

- 不做一次性大换架构。
- 每次只移动一个明确职责，并保留公开 API。
- 先拆纯逻辑和无行为变化辅助类。
- 涉及 Unity 序列化字段时保留字段名或使用 `[FormerlySerializedAs]`。
- 涉及资产/场景写入时必须先 Preview，再 Apply，再检查 git diff。

## 建议测试补强

| 优先级 | 测试方向 | 覆盖点 |
| --- | --- | --- |
| P0 | `GameEventBus` reset | SubsystemRegistration 后监听清空；重复订阅/取消订阅计数正确。 |
| P0 | JSON 导入删除保护 | Preview 有删除候选时默认 Apply 不删除；确认删除才调用 `AssetDatabase.DeleteAsset`。 |
| P1 | 波次位置策略 | 所有 `WaveDefinitionSO` 含有效 `SpawnLocationDefinition`；Wall 层缺失或策略为空时明确失败。 |
| P1 | 内容历史生命周期 | 商店、奖励、掉落、刷怪的 pick/offer 统计是否按设计共享或隔离。 |
| P1 | `GameManager` 状态机 | Menu、CharacterSelection、Game、Shop、StageComplete、GameOver 的合法迁移。 |
| P1 | `Weapon` 拆分 | 序列事件、命中窗口、投射物发射、冷却行为保持一致。 |
| P2 | DOT 结算 | Buff DOT 伤害、死亡事件、modifier 参与规则。 |

## 验证情况

- 本路线图基于本轮静态扫描、文件行数统计、高频变更统计、关键文件片段核对、工作区状态和文档/源码名称比对。
- 未执行 Unity Test Runner，未修改运行时代码。
- 本轮只新增审计报告目录下的 Markdown 文件。
