# 过时文档报告

生成时间: 2026-05-18 06:10:15 +08:00

审计范围: `Docs` 主文档、`Docs/审计` 历史报告索引风险、`Assets/Scripts` 当前实现。

结论: 文档健康度为 **中高风险**。上一轮已指出的过期类名和旧术语仍存在于非审计主文档中；本轮新增的 JSON 导入和 `SpawnLocationDefinition` 内联波次位置策略也缺少权威说明。

## 已确认冲突

| 等级 | 优先级 | 文件路径 | 问题位置 | 问题描述 | 代码实际行为 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 高 | P0 | `Docs/统一内容池系统使用指南.md` | 第 263 行附近 | 文档写刷怪运行时入口是 `WaveSpawnExecutionService`。 | 当前源码没有该类。真实链路是 `WaveManager.ProcessCurrentWaveSpawns -> WaveSpawnExecutor.Execute -> EnemyFactory.Spawn`。 | 调试刷怪权重、标签和推进倍率时会搜索不到入口。 | 替换为当前链路，并注明 `WaveSpawnExecutor` 持有刷怪内容历史。 |
| 高 | P0 | `Docs/局内推进系统设计与使用说明.md` | 第 50、68、191 行 | 文档多处引用 `WaveSpawnExecutionService`，并说它把推进快照传给 `EnemyFactory`。 | 当前实现由 `WaveManager` 创建 `WaveSpawnExecutionRequest`，`WaveSpawnExecutor` 把 `ProgressionSnapshot` 和 `WaveEnemyTag` 传给 `EnemyFactory.Spawn`。 | 局内推进是数值调参核心，入口错误会直接影响排查效率。 | 将文件路径和类名更新为 `Assets/Scripts/Waves/Services/WaveSpawnExecutor.cs`，补当前调用链。 |
| 高 | P0 | `Docs/统一内容池系统使用指南.md` | 第 282、284、387 行 | 示例和维护约束仍使用 `ContentPoolPurpose.Shop` / `ContentPoolPurpose`。 | 当前源码未发现 `ContentPoolPurpose`，实际使用 `ContentPoolScopeIds.Shop`、`Drop`、`WaveSpawn`、`WeaponReward` 等字符串常量。 | 复制示例会编译失败，也会误导新增业务池方式。 | 替换为 `ContentPoolScopeIds.Shop`，新增业务池说明改为扩展 `ContentPoolScopeIds` 或定义明确 scope 常量。 |
| 高 | P0 | `Docs/统一内容池系统使用指南.md` | 第 98 行附近 | 文档写 `ContentHistoryState` 是内容系统统一维护的运行时历史。 | 当前代码中 `ShopManager`、`RewardSelectionManager`、`DropManager`、`WaveSpawnExecutor` 各自持有 `ContentHistoryState`。 | 新功能会误以为历史跨业务共享，导致 `MaxPickCount`、上次 offer、标签选择次数理解错误。 | 改成当前事实表: 持有者、scopeId、poolId、ownerId、reset 时机、是否共享。若目标是统一历史，应标注“目标方案”。 |
| 中高 | P1 | `Docs/统一内容池系统使用指南.md` | 第 273 行 | 随机武器奖励入口写为 `AddRandomWeaponCard`。 | 当前源码未找到 `AddRandomWeaponCard`。武器奖励由 `RewardSelectionManager` 注册 `EquipmentRewardSelectionHandler.CreateWeapon()`，通过 `ContentPoolScopeIds.WeaponReward` 抽取。 | 维护者会寻找不存在的升级卡效果类，无法定位武器奖励池真实消费链路。 | 改为 `RewardSelectionManager -> EquipmentRewardSelectionHandler.CreateWeapon -> weaponRewardPool`。 |
| 中 | P1 | `Docs/伤害结算流程说明.md` | 第 190 行 | 文档写 `MeleeWeaponAttackExecutor` 命中目标。 | 当前源码未找到该类。近战命中链路为 `Weapon.TickActiveHitWindows -> HitBoxAttackExecutor.ExecuteAttack -> HitService.Apply`。 | 排查近战命中会被带向不存在类。 | 更新流程图和文字，明确 `HitBoxAttackExecutor` 是当前采样执行者。 |
| 中 | P1 | `Docs/数值系统落地规范.md` | 第 36 行 | 文档写商店从 `ContentPoolPurpose.Shop` 内容池抽取。 | `ShopManager.CreateShopRollContext` 使用 `ContentPoolScopeIds.Shop`，价格由 `ShopPricingService.GetPrice` 计算。 | 策划/程序沟通和代码搜索会使用错误术语。 | 替换为 `ContentPoolScopeIds.Shop`，保留价格由 `ShopPricingService` 统一计算的说明。 |
| 中 | P1 | `Docs/伤害结算流程说明.md`、`Docs/命中事务流水线设计方案.md` | 命中统一入口相关段落 | 文档强调命中/伤害应统一进入 `HitService` 或目标 `CombatService`。 | 新增 `DamageOverTimeFeature.OnUpdate` 直接构造 `HitResult` 并调用 `HealthComponent.ApplyHitResult`，未走 `HitService` 修饰链。 | DOT 是否应受闪避、格挡、受击修饰影响会出现实现与文档分歧。 | 明确 DOT 规则。若 DOT 要走统一结算，文档和代码都改为 `HitService.Apply`；若不走，文档标注 Buff DOT 是特殊扣血路径。 |
| 中 | P2 | `Docs/审计/2026-05-12` 到 `Docs/审计/2026-05-17` | 历史审计报告 | 历史报告保留旧问题、旧类名和旧优先级。 | 历史报告是日期快照，不应作为当前实现说明。 | 新人可能误把旧审计当最新事实。 | 新增 `Docs/审计/README.md`，说明最新日期报告优先，历史报告仅作快照。 |

## 缺失文档

| 等级 | 优先级 | 缺失项 | 影响范围 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- |
| 高 | P0 | JSON 数据导入安全说明 | `Assets/Scripts/Editor/DataImport/*`、Buff/升级卡/波次/刷怪包/饰品资产 | 当前导入工具可创建、更新、删除资产，但缺少“Preview 后如何确认删除、如何回退、哪些 JSON 是单源”的说明。 | 新增 `Docs/JSON数据导入工具使用说明.md`，要求先 Preview、检查删除候选、确认备份或 git diff 后再 Apply。 |
| 高 | P0 | 数据导入删除策略权威说明 | `BuffJsonAssetSync`、`UpgradeCardJsonAssetSync`、`WaveSpawnPackJsonAssetSync` | 多个导入器默认会删除 stale 资产，文档没有说明风险，也没有确认流程。 | 明确默认不删除、确认删除、报告落盘、引用校验、回退方式。 |
| 中高 | P1 | 波次刷怪位置策略文档 | `SpawnLocationDefinition`、`SpawnLocationResolverSettings`、三种 `SpawnLocationStrategyModel`、`WaveJsonAssetSync` | 项目已从独立 `SpawnLocationPolicySO` 转为 `WaveDefinitionSO.spawnLocation` 内联 SerializeReference，但主文档没有迁移说明。 | 新增 `Docs/波次刷怪位置策略说明.md`，说明字段、默认值、JSON 格式、Wall 层要求、地图运行时边界覆盖规则。 |
| 中高 | P1 | 内容历史生命周期权威表 | 商店、升级奖励、宝箱、武器奖励、掉落、刷怪 | 文档说统一，代码实际分散。 | 在 `Docs/统一内容池系统使用指南.md` 补表，明确每个业务池的持有者、scopeId、poolId、ownerId、reset 时机。 |
| 中高 | P1 | 场景装配契约 | `GameManager`、`WaveManager`、`GameContentBootstrap`、`UIManager`、`DropManager`、`GameInputSceneBinder` | 多个主流程对象有显式引用要求，但代码仍有查找兜底。 | 新增 `Docs/场景装配契约.md`，列出入口场景必备对象、序列化字段和启动失败诊断。 |
| 中 | P2 | 根目录 README | 新人接手、启动项目、测试、入口场景 | 当前根目录未发现 `README.md`。 | 新增 README，包含 Unity 版本 `2022.3.62f3c1`、入口场景、核心目录、测试方式、最新审计入口。 |

## 验证情况

- 已用 `rg "ContentPoolPurpose|WaveSpawnExecutionService|MeleeWeaponAttackExecutor|AddRandomWeaponCard|SpawnLocationPolicySO"` 检查主文档与源码。
- 已确认当前没有 `ContentPoolPurpose`、`WaveSpawnExecutionService`、`MeleeWeaponAttackExecutor`、`AddRandomWeaponCard` 类型。
- 已核对当前真实链路: `WaveManager -> WaveSpawnExecutor -> EnemyFactory`、`Weapon -> HitBoxAttackExecutor -> HitService`、`RewardSelectionManager -> EquipmentRewardSelectionHandler.CreateWeapon`。
- 已确认根目录缺 `README.md`，`Docs/审计` 缺 README 索引。
- 未修改原文档，只新增本报告。
