# 过时文档报告

生成时间: 2026-05-16 06:07:24 +08:00

审计范围: `Docs`、根目录入口文档、`Assets/Scripts`

结论: 当前最严重的文档问题不是路径漂移，而是“文档描述的类名、术语和历史归属与代码实际行为不一致”。`Docs/统一内容池系统使用指南.md`、`Docs/局内推进系统设计与使用说明.md`、`Docs/伤害结算流程说明.md`、`Docs/数值系统落地规范.md` 仍包含会直接误导实现的旧名称。

## 文档冲突

| 等级 | 优先级 | 文件路径 | 问题位置 | 问题描述 | 代码实际行为 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 高 | P0 | `Docs/统一内容池系统使用指南.md` | 第 96-108 行 | 文档写 `ContentHistoryState` 是内容系统统一维护的运行时历史，并要求升级卡、商店、宝箱、掉落、刷怪和武器奖励都不再维护私有抽取历史。 | 当前主源码中历史分别由 `ShopManager.contentHistoryState`、`RewardSelectionManager.contentHistoryState`、`DropManager.contentHistoryState`、`WaveSpawnExecutor.contentHistoryState` 持有。 | 新业务按文档理解会以为存在统一 per-run 历史源，但实际 pick/roll 历史按业务对象隔离，`MaxPickCount`、上一轮 offer 和标签统计会被误判。 | 先定版历史策略。若统一，新增运行局级 `RunContentHistory`；若不统一，文档改成“按业务域持有历史”，列出每个持有者、scopeId 和生命周期。 |
| 高 | P0 | `Docs/统一内容池系统使用指南.md` | 第 282-287、387 行 | 示例和维护约束仍使用 `ContentPoolPurpose.Shop` / `ContentPoolPurpose`。 | 当前源码没有 `ContentPoolPurpose`；实际使用 `ContentPoolScopeIds.Shop`、`ContentPoolScopeIds.Drop`、`ContentPoolScopeIds.WaveSpawn` 等字符串常量。 | 复制示例会编译失败，也会让维护者误以为新增业务池需要扩展枚举。 | 将示例改为 `ContentPoolScopeIds.Shop`，维护约束改为“新增业务池先扩展 `ContentPoolScopeIds` 或定义明确 scopeId 常量”。 |
| 中高 | P1 | `Docs/统一内容池系统使用指南.md` | 第 257-265 行 | 刷怪池运行时入口写为 `WaveSpawnExecutionService`。 | 当前实现是 `WaveManager.Update` 调用 `ProcessCurrentWaveSpawns`，再由 `WaveSpawnExecutor.Execute` 执行内容池抽取和 `EnemyFactory.Spawn`。 | 排查刷怪权重、标签、推进倍率时会按文档搜不到入口。 | 改成 `WaveManager -> WaveSpawnExecutor -> EnemyFactory`，并注明 `WaveSpawnExecutor` 自身持有 `ContentHistoryState`。 |
| 中高 | P1 | `Docs/统一内容池系统使用指南.md` | 第 267-275 行 | 随机武器奖励入口写为 `AddRandomWeaponCard`。 | 当前主源码未找到 `AddRandomWeaponCard`。武器奖励实际由 `EquipmentRewardSelectionHandler.CreateWeapon()`、`RewardSelectionManager` 和 `ContentPoolScopeIds.WeaponReward` 处理。 | 维护者会去寻找不存在的升级卡效果类，无法定位武器奖励池的真实消费链路。 | 改为 `RewardSelectionManager -> EquipmentRewardSelectionHandler.CreateWeapon -> WeaponRewardPool`，删除 `AddRandomWeaponCard` 入口描述。 |
| 中高 | P1 | `Docs/局内推进系统设计与使用说明.md` | 第 50、68、191 行 | 多处引用 `WaveSpawnExecutionService`，并说明其把 `RunProgressionSnapshot` 传给 `EnemyFactory`。 | 当前实现为 `WaveSpawnExecutor`，由 `WaveManager.ProcessCurrentWaveSpawns` 创建 `WaveSpawnExecutionRequest`，再通过 `enemyFactory.Spawn(..., context.ProgressionSnapshot, spawnRequest.EnemyTags)` 传递推进快照和标签。 | 局内推进与刷怪是核心数值链路，入口错误会影响调参与问题定位。 | 替换文件路径和类名，并补当前调用链。 |
| 中 | P1 | `Docs/伤害结算流程说明.md` | 第 190 行 | 文档写 `MeleeWeaponAttackExecutor` 命中目标。 | 当前源码未找到该类。近战命中链路是 `Weapon.OpenHitWindow/TickActiveHitWindows -> HitBoxAttackExecutor.ExecuteAttack -> HitService.Apply`。 | 扩展近战窗口、排查命中或伤害时会被带向不存在的类。 | 更新流程图和文字，明确 `HitBoxAttackExecutor` 是当前近战采样执行者。 |
| 中 | P1 | `Docs/数值系统落地规范.md` | 第 36 行 | 文档写商店由 `ShopManager` 从 `ContentPoolPurpose.Shop` 内容池抽取商品。 | `ShopManager.CreateShopRollContext` 实际使用 `ContentPoolScopeIds.Shop`。价格由 `ShopPricingService.GetPrice` 计算。 | 数值文档面向策划和程序协作，旧术语会导致沟通和代码搜索不一致。 | 替换为 `ContentPoolScopeIds.Shop`，并保留价格由 `ShopPricingService` 统一计算的说明。 |
| 中 | P2 | `Docs/数值资产落地实现计划.md` | 第 171-183 行 | 文档描述新增 `NumericAssetSyncWindow` 和生成 `Docs/审计/<date>/数值资产同步报告.md`，方向基本符合当前实现；但近期实际生成过 `Docs/审计/20260516_.../数值资产同步报告.md` 这类时间戳目录。 | 当前健康审计要求使用 `Docs/审计/YYYY-MM-DD/`；数值同步报告使用紧凑时间戳目录，和审计目录约定不一致。 | 多套审计/同步目录命名会让新人难以判断哪个报告是每日健康检查，哪个是数值同步产物。 | 在文档中区分“健康审计目录”和“数值同步报告目录”；如要统一，数值同步工具也改成 `YYYY-MM-DD` 下追加报告或使用 `Docs/数值同步/`。 |
| 低 | P3 | `Docs/审计/2026-05-14/*.md`、`Docs/审计/2026-05-15/*.md` | 历史审计报告 | 旧报告仍把 `AddRandomWeaponCard` 作为当前内容历史持有者之一。 | 本轮主源码未找到 `AddRandomWeaponCard`，文档中只剩旧引用。 | 历史报告有归档价值，但如果被当成当前事实，会误导修复顺序。 | 保留历史报告，不回改；新增 `Docs/审计/README.md` 或索引，说明最新报告优先，历史报告仅作快照。 |

## 缺失文档

| 等级 | 优先级 | 缺失项 | 影响范围 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- |
| 中高 | P1 | 根目录 `README.md` | 新人接手、启动项目、运行测试、入口场景 | 当前仓库没有直接说明 Unity 版本、入口场景、核心目录和常用验证命令的入口文档。 | 新增 README，至少包含 Unity 版本 `2022.3.62f3c1`、入口场景 `Assets/Scenes/Game Scene.unity`、核心目录、数值表、常用测试和最新审计入口。 |
| 中高 | P1 | 场景装配契约文档 | `GameManager`、`WaveManager`、`GameInputSceneBinder`、`UIManager`、`DropManager` | 多个系统要求场景显式引用，但部分代码仍有 `FindFirstObjectByType` 兜底。缺少“必须放什么对象、绑定什么字段”的清单。 | 新增 `Docs/场景装配契约.md`，列出 `Game Scene` 必备对象、序列化引用、允许为空的可选引用和校验方式。 |
| 中 | P2 | 当前内容池历史生命周期说明 | 商店、升级奖励、宝箱、武器奖励、掉落、刷怪 | 当前文档说统一，代码实际分散。缺少权威表说明每个业务池的历史是否共享。 | 在统一内容池文档中新增一张表: 持有者、scopeId、poolId、ownerId、reset 时机、是否跨业务共享。 |
| 中 | P2 | 数值同步工具安全说明 | `Assets/Scripts/Editor/NumericImport/*`、`Assets/GameContent/*`、`Assets/Scenes/Game Scene.unity` | `Apply Numeric Asset Sync` 会改大量资产和主场景，但缺少“先 Preview、检查哪些资产、如何回退”的操作说明。 | 新增 `Docs/数值资产同步工具使用说明.md` 或补进现有计划，明确 Preview/Apply/测试/回退流程。 |

## 验证情况

- 已用 `rg` 核对主文档中 `ContentPoolPurpose`、`WaveSpawnExecutionService`、`MeleeWeaponAttackExecutor`、`AddRandomWeaponCard` 的命中。
- 已用源码搜索确认当前没有 `ContentPoolPurpose`、`WaveSpawnExecutionService`、`MeleeWeaponAttackExecutor`、`AddRandomWeaponCard` 类型。
- 已确认 `Docs/玩法数值表.xlsx` 存在，最近修改时间为 2026-05-15 12:53:07。
- 本报告只新增文档，未修改原文档。
