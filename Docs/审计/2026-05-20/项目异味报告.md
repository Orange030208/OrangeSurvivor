# 项目异味报告

生成时间: 2026-05-20 06:08:44 +08:00

审计范围: `Assets/Scripts`、`Assets/Tests/Editor`、`Assets/GameContent`、`Assets/Scenes`、`Docs`。第三方插件、`Library`、`Temp`、Unity 自动生成工程文件只作为验证信号，不作为自研代码质量结论。

事实来源优先级: 当前磁盘源码与资产 > 构建命令输出 > `git status` > 文档。

## 总体结论

当前技术债等级: **高**。

与 2026-05-19 相比，本轮最严重的问题不是新增了某个可延期重构点，而是工作区已经进入 **构建阻断状态**。角色选择流程的删除/迁移没有闭环，导致源码仍引用已删除类型、主场景仍挂载已删除脚本、内容 Catalog 资产仍保留旧字段。此类问题会直接阻断 Unity 编译和正式入口场景启动，优先级高于常规架构重构。

## 关键异味

| 等级 | 优先级 | 文件路径 | 类 / 函数 | 问题描述 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 高 | P0 | `Assets/Scripts/UI/Instances/CharacterSelect/CharacterSelectUIPage.cs:14`、`:25`、`:71` | `CharacterSelectUIPage.OnOpeningAsync`、`OnCharacterSelectionChanged` | `CharacterSelectUIPage` 仍直接引用 `CharacterSelectionManager` 和 `CharacterSelectionChangedArgs`，但 `Assets/Scripts/Managers/CharacterSelectionManager.cs` 与 `CharacterSelectionChangedArgs.cs` 已在工作区删除。 | `dotnet build .\Survivors.sln --no-restore` 已失败，报 `CS2001` 缺失这两个源文件。即使 Unity 重新生成 csproj 后，也会因源码引用不存在类型而编译失败。 | 若角色选择已被默认角色 + 开局卡取代，删除或隔离 `CharacterSelectUIPage` 及相关事件/Prefab/Catalog 入口；若只是临时迁移，恢复 `CharacterSelectionManager` 与事件参数类型并让 `GameManager` 的状态机重新接入。 |
| 高 | P0 | `Assets/Scripts/Enemy/Brains/WormBrain.cs` | `WormBrain` | `Assembly-CSharp.csproj` 仍引用 `Assets\Scripts\Enemy\Brains\WormBrain.cs`，但当前磁盘不存在该文件，且 `Assets/Scripts/Enemy/Brains` 目录内也没有 Worm 脑类。 | 当前构建同样因 `CS2001` 失败。虽然 `.csproj` 是 Unity 生成文件且被 `.gitignore` 忽略，但这暴露了 Unity 项目文件与磁盘脚本不同步；若场景或资产仍引用 Worm 脚本，会变成 Missing Script。 | 先确认 Worm 敌人是否仍需要。需要则恢复脚本和 meta；不需要则删除对应资产/Prefab/行为树引用，并让 Unity 重新生成工程文件。随后用 `rg "WormBrain" Assets` 和 Unity 编译验证无残留。 |
| 高 | P0 | `Assets/Scenes/Game Scene.unity:25566` | 主场景组件序列化 | 主场景仍有脚本 GUID `70452e71c360ab44988c18f13506913c`，该 GUID 来自已删除的 `CharacterSelectionManager.cs.meta`。 | Unity 打开场景会出现 Missing Script。主场景残留已删除管理器会让角色选择迁移处于半完成状态，也可能破坏 Prefab/场景保存后的序列化稳定性。 | 在 Unity Editor 中打开 `Game Scene`，删除该 Missing Script 组件，并检查 `GameManager`、UI Catalog、Menu 流程是否不再需要角色选择引用。不要手写 YAML 删除，避免误伤场景对象。 |
| 高 | P0 | `Assets/GameContent/Catalog/Game Content Catalog.asset:17`；`Assets/Scripts/Content/GameContentCatalogSO.cs:19`、`:20` | `GameContentCatalogSO.DefaultCharacter`、`StarterCards` | 代码字段已改为 `defaultCharacter` 与 `starterCards`，但 Catalog 资产仍只有旧字段 `characters:`，没有 `defaultCharacter:` 或 `starterCards:`。 | `GameContentCatalogSO.ValidateCatalog` 会在 `DefaultCharacter == null` 时返回错误，`GameContentBootstrap.TryInitializeRuntime` 会阻断 Provider 安装。随后 `Player.ResolveDefaultCharacter` 得不到默认角色，开局卡流程也会拿到空列表。 | 使用 Unity Editor 或现有 `GameContentCatalogBuildUtility` 重新写入 Catalog，并确认资产 YAML 出现 `defaultCharacter:`、`starterCards:`。建议给字段加 `[FormerlySerializedAs("characters")]` 或一次性迁移脚本，避免现有角色列表无声丢失。 |
| 高 | P0 | `Assets/Scripts/Content/Editor/GameContentCatalogBuildUtility.cs:437`、`:460`、`:491`、`:557` | `SetDefaultCharacter`、`SetStarterCards`、`LoadFirstAsset` | Catalog 构建器开始填默认角色和开局卡，但它不会迁移旧 `characters` 字段，只在新字段为空时按名称搜索固定 4 张卡。 | 资产迁移依赖编辑器工具被手动运行；如果没有运行，运行时直接缺默认角色。固定卡名也把开局配置硬编码在 Editor 工具中，后续改名会静默漏配并只打 Warning。 | 把开局卡作为显式 Catalog 配置，不把卡名写死在构建器里。迁移时优先从旧 `characters` 第一项填 `defaultCharacter`，并让缺卡成为构建错误而不是可忽略 Warning。 |
| 高 | P0 | `Assets/Scripts/Managers/GameManager.cs:366`、`:668` | `EnterGameState`、`RunStarterCardSelectionAsync` | 进入 `Game` 后先打开开局卡 Popup，再开始第一波；该流程依赖 `uiManager`、`player`、`GameContentRuntime`、`starterCards` 都有效。但当前 Catalog 资产未迁移，且 `StarterCardSelectionFlow` 是普通对象，缺少统一状态机可见性。 | 若开局卡为空，当前逻辑直接跳过选择并开第一波；若 UI 流程异常，`catch` 只记录异常，不会恢复状态或给玩家可见失败路径。半完成迁移会造成“看似能进游戏但初始构筑缺失”。 | 在状态机层显式建模 `StarterCardSelection` 或 `PreRunSetup`，至少让 `RunStarterCardSelectionAsync` 对空配置打 Error 并阻断正式场景启动。给开局卡选择添加 EditMode/PlayMode 冒烟测试。 |
| 中高 | P1 | `Assets/Scripts/Player/Player.cs:45`、`:51`、`:80` | `Awake`、`Start`、`ResolveDefaultCharacter` | `Player` 在 `Awake` 和 `Start` 都尝试通过 `GameContentRuntime.TryGetProvider` 解析默认角色，并在 Provider 不可用时静默返回。 | 初始化顺序稍有变化时，`PropertiesManager`、`WeaponsHolder`、`FeatureHost` 可能已按空 `characterData` 初始化，后续再解析默认角色也不会自动重跑完整实体初始化。 | 默认角色应由生成/装配流程在实例化前注入，或 `Player.Initialize(CharacterDataSO)` 显式一次性完成；运行时兜底只用于测试并打清晰错误。 |
| 中高 | P1 | `Assets/Scripts/Event/GameEventBus.cs:25`、`:303` | `Subscribe`、`Publish`、`Clear` | 全局事件总线仍用静态字典保存监听，只有手动 `Clear()`，没有 `SubsystemRegistration` 自动清理。 | 关闭 Domain Reload、测试跨用例或某个订阅方漏退订时，旧监听会污染下一局。主流程、UI、奖励、波次都依赖事件，定位成本高。 | 增加 `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]` 自动清理，并补测试覆盖 reset 后监听数归零。 |
| 高 | P1 | `Assets/Scripts/Weapons/Weapon.cs:1` | `Weapon` | `Weapon` 约 979 行，仍承担冷却、索敌、序列、命中窗口、投射物、VFX/SFX、属性重算和 Gizmo。 | 武器是高频扩展点，新增攻击模式会继续修改同一大类，回归面覆盖战斗、表现和数值。 | 保持 MonoBehaviour 门面，先拆无行为变化的 `WeaponSequenceEventDispatcher`、`WeaponHitWindowRuntime`、`WeaponProjectileEmitter`，每步补现有武器测试。 |
| 高 | P1 | `Assets/Scripts/Managers/GameManager.cs:150`、`:161`、`:349`、`:668` | `TransitionToState`、`RunStateTransitionAsync`、`EnterGameState`、`RunStarterCardSelectionAsync` | `GameManager` 仍集中状态机、UI、音乐、暂停、玩家生成、地图、波次、商店、结算和开局卡流程。 | 角色选择迁移问题正是主流程巨型类的副作用：删除一个状态牵动 UI、Catalog、Player、场景组件、音频和文档。 | 抽纯 C# `GameStateMachine` 表达合法状态与前置流程；抽 `RunSetupFlow` 管默认角色/开局卡；`GameManager` 只做 Unity 生命周期适配。 |
| 高 | P1 | `Assets/Scripts/OrangeUIFramework/Core/Runtime/UIManager.cs:76` | `UIManager` | `UIManager` 约 1628 行，集中 Page/Popup/Modal/Tooltip、栈、遮罩、Canvas 构建、对象池和诊断。 | 所有 UI 页面都依赖它；开局卡 Popup、奖励 Popup 和角色选择页面共用同一基础设施，单点错误会扩散到全 UI。 | 不改 `IUIManager`，内部拆 `UIRootBuilder`、`UIBlockerController`、`UIViewPool`、`UIViewTransitionCoordinator`。 |
| 中高 | P1 | `Assets/Scripts/Editor/DataImport/BuffJsonAssetSync.cs:91`、`UpgradeCardJsonAssetSync.cs:89`、`WaveSpawnPackJsonAssetSync.cs:89` | `Apply` | 多个 JSON 导入器仍可直接 `AssetDatabase.DeleteAsset` 删除 JSON 中不存在的资产。 | 当前工作区已有脚本删除和 Catalog 迁移缺口，说明迁移期误删/半删风险真实存在。内容资产一旦被删会破坏序列化引用，编译未必能发现。 | 默认 Apply 禁止删除，删除改独立命令、二次确认和报告落盘；补 Editor 测试确认有删除候选时不会默认删除资产。 |
| 中 | P2 | `Assets/Scripts/Combat/HitService.cs:35`、`:41` | `CollectModifiers` | 每次命中都 `new List<IHitModifier>()` 并 `GetComponents<IHitModifierProvider>()`。 | 命中是热路径，大量子弹、DOT、范围攻击时会产生分配和组件查询成本。 | 先用 Profiler 量化；若确认热点，在实体或 FeatureHost 维护 modifier registry，Feature 变更时失效缓存。 |
| 中 | P2 | `Assets/Scripts/Features/Runtime/DamageOverTimeFeature.cs:36` | `OnUpdate` | DOT 仍直接构造 `HitResult` 并调用 `HealthComponent.ApplyHitResult`，绕过 `HitService`。 | DOT 是否受闪避、格挡、受击修饰影响与统一命中事务文档不一致。 | 定版 DOT 规则；若应统一结算，改为 `HitService.Apply`；若是特殊扣血路径，代码命名和文档要明确。 |

## 影响范围

- 编译与启动: `Assembly-CSharp` 编译、Unity 主场景、GameContentBootstrap。
- 主流程: `GameManager`、默认角色、开局卡选择、菜单开始游戏。
- UI: 角色选择旧页面、奖励 Popup 复用、UI Catalog 潜在旧入口。
- 内容资产: `Game Content Catalog.asset` 字段迁移、开局卡资产引用。
- 战斗与长期维护: `Weapon`、`GameEventBus`、导入器删除策略、DOT 命中规则。

## 验证情况

- 已运行 `dotnet build .\Survivors.sln --no-restore`，结果失败，核心错误为缺失 `WormBrain.cs`、`CharacterSelectionManager.cs`、`CharacterSelectionChangedArgs.cs`。
- 已用 `git show HEAD:...meta` 确认 `CharacterSelectionManager.cs.meta` GUID 为 `70452e71c360ab44988c18f13506913c`，并在 `Assets/Scenes/Game Scene.unity:25566` 找到残留。
- 已读取 `Assets/GameContent/Catalog/Game Content Catalog.asset`，确认只有旧字段 `characters:`，未见 `defaultCharacter:`、`starterCards:`。
- 已核查 `GameContentCatalogSO` 当前代码要求 `DefaultCharacter != null`，否则 `ValidateCatalog` 报错并阻断 Provider 安装。
- 未运行 Unity Test Runner；当前编译已阻断，测试执行前应先修复 P0 编译/迁移问题。
