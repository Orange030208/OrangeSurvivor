# 重构路线图

生成时间: 2026-05-20 06:08:44 +08:00

目标: 先恢复可编译与主场景可启动基线，再处理高耦合核心模块。路线图按风险和依赖顺序排列，不建议跳过 P0。

## 优先级总览

| 顺序 | 等级 | 优先级 | 主题 | 文件路径 | 问题描述 | 修复建议 | 完成标准 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 高 | P0 | 恢复编译基线 | `Assets/Scripts/Enemy/Brains/WormBrain.cs`、`Assets/Scripts/Managers/CharacterSelectionManager.cs`、`CharacterSelectionChangedArgs.cs`、`CharacterSelectUIPage.cs` | 当前构建因缺失源文件失败。 | 决定恢复脚本或彻底删除引用；让 Unity 重新生成 csproj。 | `dotnet build .\Survivors.sln --no-restore` 不再报缺失源文件；Unity Console 无编译错误。 |
| 2 | 高 | P0 | 收口角色选择迁移 | `GameManager.cs`、`CharacterSelectUIPage.cs`、`CharacterSelectionEvents.cs`、`Assets/Scenes/Game Scene.unity` | `GameState.CharacterSelection` 已删除，但旧 UI、事件和场景组件残留。 | 若新方案确定，删除旧页面/Catalog 入口/事件/场景 Missing Script；若保留，恢复完整状态机。 | `rg "CharacterSelectionManager|CharacterSelectionChangedArgs|GameState\\.CharacterSelection" Assets/Scripts` 无不合理残留；主场景无 Missing Script。 |
| 3 | 高 | P0 | 迁移 Game Content Catalog | `Assets/GameContent/Catalog/Game Content Catalog.asset`、`GameContentCatalogSO.cs`、`GameContentCatalogBuildUtility.cs` | 资产仍使用旧 `characters:` 字段，代码要求 `defaultCharacter` 与 `starterCards`。 | 用 Editor 工具或迁移脚本写入新字段；必要时加 `[FormerlySerializedAs("characters")]`。 | Catalog 校验通过；资产中存在 `defaultCharacter:` 和非空 `starterCards:`。 |
| 4 | 高 | P0 | 稳定默认角色和开局卡数据流 | `Player.cs`、`GameManager.cs`、`StarterCardSelectionFlow.cs` | `Player` 运行时自行从全局 Provider 解析角色，开局卡为空会静默跳过。 | 建立 `RunSetupFlow` 或 `PlayerSpawnContext`，主流程显式解析并注入默认角色和开局卡。 | 缺默认角色/开局卡时正式场景启动失败并给明确错误；测试场景有显式降级。 |
| 5 | 高 | P0 | 修正文档旧入口 | `Docs/统一内容池系统使用指南.md`、`Docs/局内推进系统设计与使用说明.md`、`Docs/伤害结算流程说明.md`、`Docs/数值系统落地规范.md` | 主文档仍引用不存在类名和旧术语。 | 替换为当前真实链路和 `ContentPoolScopeIds`。 | `rg "ContentPoolPurpose|WaveSpawnExecutionService|MeleeWeaponAttackExecutor|AddRandomWeaponCard" Docs -g "*.md" -g "!Docs/审计/**"` 无当前实现误导。 |
| 6 | 高 | P0 | 建立场景装配契约 | `Docs/场景装配契约.md`、`GameContentBootstrap.cs`、主场景 | 主场景必备对象和 Catalog 必填字段没有权威说明。 | 新增文档和 Editor 校验清单。 | 文档列出 `GameContentBootstrap`、`GameManager`、UI、输入、默认角色、开局卡、波次必填项。 |
| 7 | 中高 | P1 | 事件总线自动 reset | `Assets/Scripts/Event/GameEventBus.cs`、`Assets/Tests/Editor/*` | 静态监听字典只靠手动 `Clear()`。 | 增加 `SubsystemRegistration` reset。 | EditMode 测试覆盖 reset 后监听数为 0。 |
| 8 | 高 | P1 | 数据导入删除保护 | `Assets/Scripts/Editor/DataImport/*JsonAssetSync.cs` | Apply 可直接删除资产。 | 默认禁止删除，删除走二次确认和报告。 | 有 deleteCandidates 时默认 Apply 不删除资产；测试覆盖。 |
| 9 | 高 | P1 | 拆分 GameManager 主流程 | `Assets/Scripts/Managers/GameManager.cs` | 状态机、UI、开局卡、波末、暂停都在一个类。 | 抽 `GameStateMachine`、`RunSetupFlow`、`RunLifecycleCoordinator`。 | 状态迁移单测覆盖 `Menu -> Game -> StarterCardSelection -> FirstWave -> Shop/StageComplete/GameOver`。 |
| 10 | 高 | P1 | 拆分 Weapon 运行时 | `Assets/Scripts/Weapons/Weapon.cs` | 武器运行时单类过大。 | 先拆序列事件、命中窗口、投射物发射，不改外部 API。 | 现有武器测试通过，`Weapon` 行数和职责明显下降。 |
| 11 | 中高 | P1 | 拆 UIManager 内部基础设施 | `Assets/Scripts/OrangeUIFramework/Core/Runtime/UIManager.cs` | UI 核心单类过大。 | 保持 `IUIManager`，内部拆 Builder、Blocker、Pool、Diagnostics。 | UI 框架测试通过，页面/Popup/Modal 行为不变。 |
| 12 | 中高 | P1 | 定版内容历史生命周期 | `ContentHistoryState.cs`、`ShopManager.cs`、`RewardSelectionManager.cs`、`WaveSpawnExecutor.cs` | 文档说统一，代码实际分散。 | 决定 per-run 共享或 per-domain 隔离，并写文档。 | 内容池历史行为有表格和测试。 |
| 13 | 中 | P2 | 统一 DOT 伤害口径 | `DamageOverTimeFeature.cs`、`HitService.cs`、伤害文档 | DOT 绕过 `HitService`。 | 决策 DOT 是否受命中修饰，再改代码或文档。 | DOT 行为有测试覆盖。 |
| 14 | 中 | P2 | 分阶段引入 asmdef | `Assets/Scripts`、`Assets/Tests/Editor` | 模块边界只靠目录约定。 | 从 `Combat`、`Content`、`Pooling`、`OrangeUIFramework`、`Tests` 开始。 | 无循环依赖，测试程序集引用明确。 |

## 推荐执行批次

### 批次 A: 恢复可编译与可启动

1. 修复缺失脚本引用: `WormBrain`、`CharacterSelectionManager`、`CharacterSelectionChangedArgs`。
2. 清理主场景 Missing Script。
3. 迁移 `Game Content Catalog.asset` 的 `defaultCharacter` 与 `starterCards`。
4. 重新生成 Unity 工程文件并编译。

### 批次 B: 让迁移可维护

1. 写场景装配契约和默认角色/开局卡说明。
2. 给 Catalog 校验补 Editor 测试。
3. 给开局卡选择补一个最小流程测试或 PlayMode 冒烟。

### 批次 C: 降低后续回归面

1. 给 `GameEventBus` 加自动 reset。
2. 给数据导入器加删除保护。
3. 拆 `GameManager` 的状态机和开局准备流程。
4. 再拆 `Weapon` 与 `UIManager`。

## 回归测试建议

| 测试主题 | 推荐覆盖 |
| --- | --- |
| 编译基线 | Unity Console 无编译错误，`dotnet build` 不报缺失源文件。 |
| 主流程 | `Menu -> Game -> StarterCardSelection -> FirstWave -> Shop -> Game`，以及 `GameOver` / `StageComplete`。 |
| Catalog | 缺默认角色、缺开局卡、重复开局卡、旧字段迁移。 |
| 场景装配 | 主场景无 Missing Script，必填引用缺失时报明确错误。 |
| 数据导入 | Preview 有删除候选时默认 Apply 不删除。 |
| 事件总线 | Subsystem reset 后监听数为 0，重复进场景不重复触发。 |

## 本轮验证情况

- 已执行 `dotnet build .\Survivors.sln --no-restore`，当前失败，不能进入后续测试批次。
- 已确认主场景残留已删除 `CharacterSelectionManager` 脚本 GUID。
- 已确认 Catalog 资产字段未迁移。
- 未修改业务代码或资产；本路线图用于后续修复执行。
