# 过时文档报告

生成时间: 2026-05-20 06:08:44 +08:00

审计范围: `Docs` 主文档、历史审计报告、当前 `Assets/Scripts` 与 `Assets/GameContent` 实现。

文档健康度: **高风险**。主文档仍保留连续多轮已指出的旧类名和旧术语；同时本轮角色选择流程迁移后，文档和历史路线图仍默认存在 `CharacterSelection` 状态，已经落后于当前代码。

## 已确认冲突

| 等级 | 优先级 | 文件路径 | 问题位置 | 问题描述 | 代码实际行为 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 高 | P0 | `Docs/统一内容池系统使用指南.md:263` | 刷怪池运行时入口 | 文档写运行时入口是 `WaveSpawnExecutionService`。 | 当前源码没有该类；真实链路是 `WaveManager.ProcessCurrentWaveSpawns -> WaveSpawnExecutor.Execute -> EnemyFactory.Spawn`。 | 调试刷怪权重、标签、推进倍率时会按文档搜索不到入口。 | 替换为 `WaveSpawnExecutor`，并补当前调用链和历史持有者说明。 |
| 高 | P0 | `Docs/局内推进系统设计与使用说明.md:50`、`:68`、`:191` | 推进快照传递 | 文档多处引用 `WaveSpawnExecutionService`，并说它把 `RunProgressionSnapshot` 传给 `EnemyFactory`。 | 当前由 `WaveManager` 创建请求，`WaveSpawnExecutor` 把 `ProgressionSnapshot` 与 `WaveEnemyTag` 传给 `EnemyFactory.Spawn`。 | 局内推进是数值调参核心，入口名错误会直接降低排查效率。 | 更新类名、文件路径、流程图与责任边界。 |
| 高 | P0 | `Docs/统一内容池系统使用指南.md:282`、`:284`、`:387` | 内容池 scope 示例 | 文档示例和维护约束仍使用 `ContentPoolPurpose.Shop` / `ContentPoolPurpose`。 | 当前源码未发现 `ContentPoolPurpose`，实际使用 `ContentPoolScopeIds.Shop`、`Drop`、`WaveSpawn`、`WeaponReward` 等字符串常量。 | 复制示例会编译失败，也会误导新增业务池的扩展方式。 | 替换为 `ContentPoolScopeIds`，并说明新增业务池应定义稳定 scopeId 常量。 |
| 高 | P0 | `Docs/数值系统落地规范.md:36` | 商店内容池描述 | 文档写商店从 `ContentPoolPurpose.Shop` 内容池抽取。 | `ShopManager.CreateShopRollContext` 使用 `ContentPoolScopeIds.Shop`，价格由 `ShopPricingService.GetPrice` 计算。 | 策划/程序沟通会沿用旧术语，代码搜索与配置说明不一致。 | 改为 `ContentPoolScopeIds.Shop`，保留价格服务说明。 |
| 中高 | P1 | `Docs/统一内容池系统使用指南.md:273` | 武器奖励入口 | 文档写随机武器奖励入口为 `AddRandomWeaponCard`。 | 当前源码未找到 `AddRandomWeaponCard`；武器奖励由 `RewardSelectionManager` 注册 `EquipmentRewardSelectionHandler.CreateWeapon()`，使用 `ContentPoolScopeIds.WeaponReward`。 | 维护者会追不存在的升级卡效果类，无法定位真实武器奖励链路。 | 改为当前真实链路，并说明武器奖励历史 scope。 |
| 中 | P1 | `Docs/伤害结算流程说明.md:190` | 近战命中流程 | 文档写 `MeleeWeaponAttackExecutor` 命中目标。 | 当前源码未找到该类；近战链路是 `Weapon.TickActiveHitWindows -> HitBoxAttackExecutor.ExecuteAttack -> HitService.Apply`。 | 排查近战窗口、命中盒或伤害时会被带到不存在类。 | 更新流程图和示例类名。 |
| 中 | P1 | `Docs/伤害结算流程说明.md`、`Docs/命中事务流水线设计方案.md` | 统一命中入口 | 文档强调伤害统一进入 `HitService`。 | `DamageOverTimeFeature.OnUpdate` 直接调用 `HealthComponent.ApplyHitResult`，未走 `HitService` 修饰链。 | DOT 是否受闪避、格挡、受击修饰影响不清晰。 | 定版 DOT 规则；代码或文档必须二选一修正。 |
| 高 | P0 | `Docs/审计/2026-05-19/重构路线图.md:60`、`Docs/审计/2026-05-18/重构路线图.md:81` | GameManager 状态机测试建议 | 历史路线图仍把 `CharacterSelection` 作为当前状态机路径。 | 当前 `GameState` 已移除 `CharacterSelection`，`OnMenuStartClicked` 直接转 `Game`，但角色选择 UI 残留导致编译失败。 | 历史审计作为快照可以保留，但若被当成当前事实会误导修复方向。 | 新增 `Docs/审计/README.md` 说明最新日期报告优先；本轮路线图改为 `Menu -> Game -> StarterCardSelection -> FirstWave` 的迁移闭环。 |
| 高 | P0 | 缺失文档 | 默认角色/开局卡迁移说明 | 当前没有文档说明角色选择被默认角色 + 开局卡取代，也没有 Catalog 字段迁移步骤。 | 代码已经引入 `DefaultCharacter`、`StarterCards`、`StarterCardSelectionFlow`，资产仍未迁移。 | 后续维护者不知道该删旧角色选择页面，还是恢复角色选择状态；容易继续半迁移。 | 新增 `Docs/默认角色与开局卡流程说明.md` 或更新场景装配契约，写清启动流程、Catalog 必填字段、旧页面处理策略。 |

## 缺失文档

| 等级 | 优先级 | 缺失项 | 影响范围 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- |
| 高 | P0 | 根目录 `README.md` | 新人接手、自动化任务、启动项目、测试入口 | 当前根目录没有可信入口文档；新人只能从专题文档和历史审计中拼项目现状。 | 新增 README，包含 Unity 版本、入口场景、核心目录、测试命令、当前审计入口和已知构建状态。 |
| 高 | P0 | 场景装配契约 | `GameContentBootstrap`、`GameManager`、`UIManager`、`WaveManager`、默认角色、开局卡 | 主场景已经出现 Missing Script 和 Catalog 字段迁移缺口，说明装配约束没有文档化。 | 新增 `Docs/场景装配契约.md`，列必备对象、字段、Catalog 必填项、验证方式。 |
| 高 | P0 | 默认角色与开局卡流程说明 | `GameContentCatalogSO`、`Player`、`GameManager`、`StarterCardSelectionFlow`、旧角色选择 UI | 当前代码和资产处于半迁移状态，缺少权威流程说明。 | 明确 `Menu -> Game -> PreRunSetup/StarterCardSelection -> FirstWave`，列出旧 `CharacterSelection` 的删除或保留策略。 |
| 高 | P0 | JSON 数据导入安全说明 | `Assets/Scripts/Editor/DataImport/*JsonAssetSync.cs` | 多个导入器可删除资产，但没有操作说明、确认流程和回退说明。 | 新增导入工具文档，要求 Preview、检查删除候选、确认 git diff/备份后再执行删除。 |
| 中高 | P1 | 内容历史生命周期权威表 | 商店、升级奖励、宝箱、武器奖励、掉落、刷怪 | 文档说统一历史，代码实际分散持有 `ContentHistoryState`。 | 在内容池指南中补持有者、scopeId、poolId、ownerId、reset 时机。 |
| 中高 | P1 | 波次刷怪位置策略说明 | `WaveDefinitionSO.spawnLocation`、`SpawnLocationDefinition`、`SpawnLocationResolverSettings` | 项目已使用 SerializeReference 内联位置策略，但主文档缺字段与资产验证说明。 | 新增字段说明、默认值、JSON 导入格式、Wall 层要求和验证工具建议。 |

## 建议修复顺序

1. 先写或更新 `Docs/场景装配契约.md`，把默认角色、开局卡、主场景必备对象和 Missing Script 检查列为 P0。
2. 更新内容池、局内推进、伤害结算三份主文档，删除 `ContentPoolPurpose`、`WaveSpawnExecutionService`、`MeleeWeaponAttackExecutor`、`AddRandomWeaponCard` 的当前实现描述。
3. 新增 `Docs/审计/README.md`，说明历史报告是日期快照，最新日期报告优先。
4. 新增根 README，作为项目入口，而不是让新人从审计报告反推项目结构。

## 验证情况

- 已用 `rg "WaveSpawnExecutionService|ContentPoolPurpose|MeleeWeaponAttackExecutor|AddRandomWeaponCard|CharacterSelection"` 核对 `Docs` 与源码。
- 已确认当前源码没有 `ContentPoolPurpose`、`WaveSpawnExecutionService`、`MeleeWeaponAttackExecutor`、`AddRandomWeaponCard`。
- 已确认当前 `GameState` 已移除 `CharacterSelection`，但旧 UI 代码仍引用已删除类型。
- 已确认根目录没有 `README.md`，`Docs/审计` 没有 README 索引。
