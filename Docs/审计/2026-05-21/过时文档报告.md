# 过时文档报告

生成时间: 2026-05-21 06:15:26 +08:00

审计范围: `Docs` 主文档、历史审计报告索引状态、当前 `Assets/Scripts` 与 `Assets/GameContent` 实现。

文档健康度: **中高风险**。

当前代码已经从 2026-05-20 的编译阻断状态恢复，但主文档仍保留旧类名、旧入口和旧术语。文档落后主要集中在内容池、局内推进、伤害结算、场景装配和默认角色/开局卡流程。

## 已确认冲突

| 等级 | 优先级 | 文件路径 | 问题位置 | 问题描述 | 代码实际行为 | 风险分析 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 高 | P0 | `Docs/统一内容池系统使用指南.md:263` | 刷怪池运行时入口 | 文档写运行时入口为 `WaveSpawnExecutionService`。 | 当前源码没有该类；真实入口是 `WaveManager.ProcessCurrentWaveSpawns -> WaveSpawnExecutor.Execute -> EnemyFactory.Spawn`。 | 排查刷怪权重、标签或推进倍率时会按文档搜索不到入口。 | 替换为 `WaveSpawnExecutor`，并写清 `WaveManager` 如何创建 `WaveSpawnExecutionRequest`。 |
| 高 | P0 | `Docs/局内推进系统设计与使用说明.md:50`、`:68`、`:191` | 推进快照传递 | 文档多处引用 `WaveSpawnExecutionService`，并称它传递 `RunProgressionSnapshot`。 | 当前 `WaveSpawnExecutor.CreateWaveRollContext` 将 `modifierContext.SpawnContext.ProgressionSnapshot` 放入 `ContentRollContext`，并由 `EnemyFactory.Spawn` 接收。 | 数值调参和刷怪排查入口错误，会直接降低维护效率。 | 更新类名、文件路径和流程图，改为 `WaveSpawnExecutor` 链路。 |
| 高 | P0 | `Docs/统一内容池系统使用指南.md:282`、`:284`、`:387` | 内容池 scope 示例 | 文档仍使用 `ContentPoolPurpose.Shop` 和“扩展 `ContentPoolPurpose`”。 | 当前源码使用 `ContentPoolScopeIds.Shop`、`Drop`、`WaveSpawn`、`WeaponReward` 等字符串常量；没有 `ContentPoolPurpose` 类型。 | 复制文档示例会编译失败，新增业务池也会走错扩展点。 | 全部替换为 `ContentPoolScopeIds`；新增 scope 说明应落到 `ContentPoolScopeIds`。 |
| 高 | P0 | `Docs/数值系统落地规范.md:36` | 商店内容池描述 | 文档写商店从 `ContentPoolPurpose.Shop` 内容池抽取。 | `ShopManager.CreateShopRollContext` 使用 `ContentPoolScopeIds.Shop`，价格由 `ShopPricingService.GetPrice` 计算。 | 策划和程序对术语理解不一致，配置沟通成本上升。 | 改为 `ContentPoolScopeIds.Shop`，保留价格服务说明。 |
| 中高 | P1 | `Docs/统一内容池系统使用指南.md:273` | 武器奖励入口 | 文档写随机武器奖励入口是 `AddRandomWeaponCard`。 | 当前未找到该类/入口；武器奖励由 `RewardSelectionManager` 注册 `EquipmentRewardSelectionHandler.CreateWeapon()`，使用 `ContentPoolScopeIds.WeaponReward`。 | 维护者会追踪不存在的升级卡效果类，无法定位真实奖励链路。 | 改为 `RewardSelectionManager -> EquipmentRewardSelectionHandler.CreateWeapon -> ContentPoolScopeIds.WeaponReward`。 |
| 中 | P1 | `Docs/伤害结算流程说明.md:190` | 近战命中流程 | 文档写 `MeleeWeaponAttackExecutor` 命中目标。 | 当前未找到该类；近战链路是 `Weapon.TickActiveHitWindows -> HitBoxAttackExecutor.ExecuteAttack -> HitService.Apply`。 | 排查近战窗口、命中盒或伤害时会被带到不存在类。 | 更新流程图和调用链示例。 |
| 中 | P1 | `Docs/伤害结算流程说明.md`、`Docs/命中事务流水线设计方案.md` | 统一命中入口 | 文档强调伤害统一进入 `HitService`。 | `DamageOverTimeFeature.OnUpdate` 直接调用 `HealthComponent.ApplyHitResult`，未走 `HitService` 修饰链。 | DOT 规则不清：到底是普通命中、Buff 真实伤害，还是特殊结算。 | 定版 DOT 规则后同步文档。若绕过是设计，文档必须列为例外。 |
| 高 | P0 | 缺失文档 | 默认角色与开局卡流程 | 当前源码和 Catalog 已有 `defaultCharacter`、`starterCards`、`StarterCardSelectionFlow`，但非审计主文档没有说明这套启动流程。 | `GameManager.EnterGameState` 第一次进 `Game` 后打开开局卡选择，再启动第一波；`Player` 仍有默认角色运行时兜底。 | 新人不知道角色选择旧流程是否已废弃，也不知道 Catalog 哪些字段是正式入口必填。 | 新增或更新 `Docs/默认角色与开局卡流程说明.md`，写清 `Menu -> Game -> StarterCardSelection -> FirstWave`。 |
| 高 | P0 | 缺失文档 | 场景装配契约 | 当前没有主场景必备对象/引用说明。 | `GameContentBootstrap`、`GameManager`、`UIManager`、`WaveManager`、`EnemyRegistry`、`Player`、Catalog、UI Catalog 都有运行时硬要求。 | 场景或 Prefab 改动后，缺引用会在运行时才暴露。 | 新增 `Docs/场景装配契约.md`，列必备对象、Inspector 字段、Catalog 必填项和验证方式。 |
| 中高 | P1 | 缺失文档 | JSON 导入安全说明 | 多个导入器 `Apply` 会删除 JSON 中不存在的资产。 | `BuffJsonAssetSync`、`UpgradeCardJsonAssetSync`、`WaveSpawnPackJsonAssetSync` 在 `Apply` 中调用 `AssetDatabase.DeleteAsset`。 | 操作员不知道 Preview 的删除候选在 Apply 中会真实删除。 | 新增导入工具文档，要求 Preview、审查删除候选、确认 git diff/备份后再允许删除。 |
| 中 | P2 | 缺失文档 | 内容历史生命周期权威表 | 文档讲“统一内容池”，但历史状态实际分散在商店、掉落、奖励、刷怪。 | `ShopManager`、`RewardSelectionManager`、`DropManager`、`WaveSpawnExecutor` 各有 `ContentHistoryState`。 | 内容去重和保底行为难以预测，策划可能以为所有池共享历史。 | 在内容池指南中补 scopeId、poolId、ownerId、持有者、reset 时机表。 |
| 中 | P2 | 项目根 | `README.md` 缺失 | 根目录未发现 `README*`。 | 当前项目入口信息散落在专题文档和历史审计中。 | 新人接手成本高，自动化任务也缺少快速验证入口。 | 新增根 README，包含 Unity 版本、入口场景、测试/构建命令、核心目录和最新审计入口。 |

## 历史文档处理建议

- `Docs/审计/YYYY-MM-DD/*` 属于日期快照，不建议直接改历史内容。
- 建议新增 `Docs/审计/README.md`，说明“历史审计只代表生成日状态，最新日期报告优先”。
- 主文档应尽快从历史审计结论中回填修正，避免每轮报告重复指出相同旧入口。

## 缺失文档优先级

| 等级 | 优先级 | 缺失项 | 影响范围 | 修复建议 |
| --- | --- | --- | --- | --- |
| 高 | P0 | 场景装配契约 | 主场景、Catalog、UI、输入、波次、玩家生成 | 新增 `Docs/场景装配契约.md`。 |
| 高 | P0 | 默认角色与开局卡流程 | `GameContentCatalogSO`、`Player`、`GameManager`、`StarterCardSelectionFlow` | 新增流程说明和必填字段清单。 |
| 高 | P0 | 内容池/局内推进旧入口修订 | 刷怪、商店、奖励、内容池配置 | 更新现有三份主文档。 |
| 中高 | P1 | JSON 导入安全说明 | Buff、升级卡、刷怪包资产 | 写 Preview/Apply/删除确认流程。 |
| 中 | P2 | 根 README | 新人接手、自动化检查 | 新增入口文档。 |

## 验证情况

- 已用 `rg "WaveSpawnExecutionService|ContentPoolPurpose|MeleeWeaponAttackExecutor|AddRandomWeaponCard|CharacterSelection" Docs -g "*.md" -g "!Docs/审计/**"` 核对主文档。
- 已确认当前源码未找到 `WaveSpawnExecutionService`、`ContentPoolPurpose`、`MeleeWeaponAttackExecutor`、`AddRandomWeaponCard`。
- 已确认 `Assets/GameContent/Catalog/Game Content Catalog.asset` 当前存在 `defaultCharacter` 和 4 个 `starterCards`，但主文档没有对应流程说明。
- 已确认根目录没有 `README*`。
