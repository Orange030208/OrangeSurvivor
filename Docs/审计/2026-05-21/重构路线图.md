# 重构路线图

生成时间: 2026-05-21 06:15:26 +08:00

目标: 当前编译已恢复，下一阶段应先修可触发的高风险逻辑，再降低核心大类和文档腐化带来的长期维护成本。

## 优先级总览

| 顺序 | 等级 | 优先级 | 主题 | 文件路径 | 问题描述 | 修复建议 | 完成标准 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 高 | P0 | 修复显示确认二次变更风险 | `Assets/Scripts/UI/Instances/Child/SettingsPanelManager.cs` | `RequestDisplayConfirmationAsync` 先应用目标显示设置，再判断 `displayConfirmationPending`。 | pending 判断前置；确认期间禁用显示控件或合并最后请求。 | 连续两次修改分辨率/窗口模式不会绕过确认；测试覆盖。 |
| 2 | 高 | P0 | 修正文档旧入口 | `Docs/统一内容池系统使用指南.md`、`Docs/局内推进系统设计与使用说明.md`、`Docs/伤害结算流程说明.md`、`Docs/数值系统落地规范.md` | 主文档引用不存在的 `WaveSpawnExecutionService`、`ContentPoolPurpose`、`MeleeWeaponAttackExecutor`、`AddRandomWeaponCard`。 | 替换为当前真实链路。 | `rg "WaveSpawnExecutionService|ContentPoolPurpose|MeleeWeaponAttackExecutor|AddRandomWeaponCard" Docs -g "*.md" -g "!Docs/审计/**"` 无当前实现误导。 |
| 3 | 高 | P0 | 建立场景装配契约 | `Docs/场景装配契约.md` | 主场景必备对象、Catalog 必填字段、UI/输入/波次依赖没有权威文档。 | 新增契约文档。 | 文档列出 `GameContentBootstrap`、`GameManager`、`UIManager`、`WaveManager`、`EnemyRegistry`、默认角色、开局卡、测试方式。 |
| 4 | 高 | P0 | 写默认角色与开局卡流程说明 | `Docs/默认角色与开局卡流程说明.md` | 代码和 Catalog 已迁移，但主文档没有解释新流程。 | 写 `Menu -> Game -> StarterCardSelection -> FirstWave`，说明旧角色选择已移除。 | 新人能按文档判断 Catalog 字段和流程入口。 |
| 5 | 中高 | P1 | 事件总线自动 reset | `Assets/Scripts/Event/GameEventBus.cs`、`Assets/Tests/Editor/*` | 静态事件字典只靠手动 `Clear()`。 | 增加 `SubsystemRegistration` reset。 | EditMode 测试覆盖 reset 后监听数为 0。 |
| 6 | 高 | P1 | 数据导入删除保护 | `Assets/Scripts/Editor/DataImport/BuffJsonAssetSync.cs`、`UpgradeCardJsonAssetSync.cs`、`WaveSpawnPackJsonAssetSync.cs` | Apply 默认删除 stale assets。 | 新增 `ApplyOptions`，默认禁止删除；删除需要确认和引用扫描。 | 有 deleteCandidates 时默认 Apply 不删除资产；测试覆盖。 |
| 7 | 中高 | P1 | 稳定默认角色数据流 | `Assets/Scripts/Player/Player.cs`、`Assets/Scripts/Managers/GameManager.cs` | `Player` 在生命周期里自行从 Provider 解析角色。 | 增加 `Player.Initialize(CharacterDataSO)` 或 `PlayerSpawnContext`。 | 正式场景由主流程显式注入角色；缺默认角色阻断启动。 |
| 8 | 高 | P1 | 拆分 `GameManager` 主流程 | `Assets/Scripts/Managers/GameManager.cs` | 状态机、UI、波次、暂停、开局卡、结算集中。 | 抽 `GameStateMachine`、`RunSetupFlow`、`RunLifecycleCoordinator`。 | 状态迁移测试覆盖 `Menu -> Game -> StarterCardSelection -> FirstWave -> Shop/StageComplete/GameOver`。 |
| 9 | 高 | P1 | 拆分 `Weapon` 运行时 | `Assets/Scripts/Weapons/Weapon.cs` | 武器运行时单类过大。 | 先拆序列事件、命中窗口、投射物发射、Gizmo，不改外部 API。 | 现有武器测试通过，`Weapon` 职责明显下降。 |
| 10 | 高 | P1 | 拆 UIManager 内部基础设施 | `Assets/Scripts/OrangeUIFramework/Core/Runtime/UIManager.cs` | UI 核心单类过大。 | 保持 `IUIManager`，拆 Builder、Blocker、Pool、Diagnostics。 | UI 框架测试通过，页面/Popup/Modal 行为不变。 |
| 11 | 中高 | P1 | 定版内容历史生命周期 | `ShopManager.cs`、`RewardSelectionManager.cs`、`DropManager.cs`、`WaveSpawnExecutor.cs`、内容池文档 | 历史状态分散，文档没有说明隔离策略。 | 决定 per-run 共享或 per-domain 隔离，并补 scope 表。 | 内容池历史行为有文档和测试。 |
| 12 | 中 | P2 | 统一 DOT 伤害口径 | `DamageOverTimeFeature.cs`、`HitService.cs`、伤害文档 | DOT 绕过 `HitService`。 | 决策 DOT 是否受命中修饰，再改代码或文档。 | DOT 行为有测试覆盖。 |
| 13 | 中 | P2 | 分阶段引入 asmdef | `Assets/Scripts`、`Assets/Tests/Editor` | 模块边界主要靠目录约定。 | 从 `Combat`、`Content`、`Pooling`、`OrangeUIFramework`、`Tests` 开始。 | 无循环依赖，测试程序集引用明确。 |

## 推荐执行批次

### 批次 A: 立即降低真实风险

1. 修复 `SettingsPanelManager.RequestDisplayConfirmationAsync` 的 pending 判断顺序。
2. 保存并 Kill `OnRebindClicked` 中的延迟刷新 Tween。
3. 给 `GameEventBus` 加 Subsystem reset。
4. 给 JSON 导入器加默认不删除保护。

### 批次 B: 恢复文档可信度

1. 更新内容池、局内推进、伤害、数值系统文档中的旧类名。
2. 新增场景装配契约。
3. 新增默认角色与开局卡流程说明。
4. 新增 `Docs/审计/README.md`，说明历史报告是快照。

### 批次 C: 降低核心回归面

1. 显式化默认角色注入。
2. 拆 `GameManager` 的状态机和开局准备流程。
3. 拆 `Weapon` 的攻击序列事件、命中窗口和投射物发射。
4. 拆 `UIManager` 的根构建、遮罩、池化和诊断。

## 回归测试建议

| 测试主题 | 推荐覆盖 |
| --- | --- |
| 设置显示确认 | 连续修改分辨率/窗口模式、确认、取消、关闭弹窗、对象销毁。 |
| 编译基线 | `dotnet build .\Survivors.sln --no-restore` 无错误。 |
| 事件总线 | Subsystem reset 后监听数为 0，重复进场景不重复触发。 |
| 数据导入 | Preview 有删除候选时默认 Apply 不删除；显式允许删除时输出报告。 |
| 主流程 | `Menu -> Game -> StarterCardSelection -> FirstWave -> Shop -> Game`，以及 `GameOver` / `StageComplete`。 |
| Catalog | 缺默认角色、缺开局卡、重复开局卡、缺内容池时错误可定位。 |
| 武器 | 近战窗口、投射物序列事件、SFX/VFX 事件、波末清理。 |
| 文档 | 旧入口 `rg` 检查作为文档 CI 的最低门槛。 |

## 本轮验证情况

- 已执行 `dotnet build .\Survivors.sln --no-restore`，成功，0 错误。
- 已确认 Catalog 资产存在 `defaultCharacter` 与 4 个 `starterCards`。
- 已确认主文档仍存在旧入口和旧术语。
- 未修改业务代码或资产；本路线图用于后续修复执行。
