---
description: 仅在涉及游戏状态、UI 分层、事件职责、运行时配置、模拟暂停或武器系统时使用；用于约束系统边界，不用于一般代码风格或通用开发规范。
alwaysApply: false
---

# Survivors 系统规则

## 职责边界

- UI 只发事件、收快照、更新表现；不直接切 `GameState`、开关页面或修改 `Time.timeScale`。
- `GameManager` 负责全局状态、暂停恢复、返回菜单、波次推进等状态决策。
- `GameStateUIController` 只把状态事件翻译为页面开关，不承载业务。
- 需要“先关 UI 再执行业务”的流程，放到独立 flow/coordinator。
- 业务管理器消费 UI 意图事件，UIPage 不直接改业务对象。

## UI 系统

- 页面激活分为 `VisualActive` 与 `InputActive`。
- 页面交互语义由 `UIManager` + `UIPageBase` 统一控制，动画组件不得改页面级交互状态。
- `UIRevealMotion` 只负责 reveal；`UISequenceDirector` 只负责序列动画。
- UI 不缓存业务态；需要当前值时通过快照事件请求并直接刷新。
- 详情类 UI 统一消费描述列表，不直接依赖运行时 feature 安装接口。

## 描述与运行时特性

- `IDescriptionSource` 只负责提供展示描述。
- `IRuntimeFeatureSource` 只负责提供运行时特性。
- 展示层只依赖描述；运行时特性系统只依赖 feature source。
- `WeaponDataSO` 默认只作描述来源，不作 `IRuntimeFeatureSource`。
- 同时承担展示与运行时特性的对象，代码职责必须分离。

## 事件约定

- 明确业务完成优先使用完成事件，如 `*CompletedEvent`。
- UI 点击用 `*ClickedEvent`；快照请求用 `Request*SnapshotEvent`；业务动作请求用 `*RequestedEvent`。
- 状态变化统一使用 `*ChangedEvent`。
- 可扩展业务数据事件必须带类型信息，优先使用通用载荷事件。

## 运行时配置

- 外部装配/注入决定的值视为运行时可变配置。
- 运行时可变配置不要在构造、字段初始化或 `Awake()` 中缓存最终值。
- 优先在实际使用点读取；必须提前注入时，由装配层在实例化后显式下发。
- 辅助对象默认只缓存稳定引用，不缓存后续可能变化的配置。

## 模拟暂停

- 只有 `GameState.Game` 且未暂停时，游戏模拟才允许推进。
- 是否推进模拟由 `GameManager` 统一决定；其他系统不直接改 `Time.timeScale`。
- 非战斗界面允许继续播放的内容仅限 UI/转场表现，并应使用 unscaled time。
- 发现系统在 `Game` 外继续推进时，优先修正时间源或状态门禁。

## 武器系统

- `Weapon` 负责通用索敌、攻击冷却、朝向更新、运行时属性计算。
- `MeleeWeapon` / `RangeWeapon` 只负责各自攻击实现，不把细节塞回基类。
- 武器索敌以持有者 `Entity` 为主体，通过 `EntityExtensions.FindClosestTargetInRange(...)` 查询。
- 无目标时，武器朝向只依赖持有者 `Entity.IsMoving` 与 `CurrentFacingDirection`。
- `PlayerController` 只负责输入并写入 `Player.ApplyMoveDirection(...)`。
- `WeaponSequenceBridge` 只负责桥接；`WeaponMotionSequencePlayer` 只负责关键帧采样与事件推进。
- 固定帧是本地绝对位移；动态帧才按目标方向与攻击半径解算，二者不得混用。
- 武器进入 `IsAttacking` 后保持当前攻击方向，攻击结束后再过渡到新方向。
