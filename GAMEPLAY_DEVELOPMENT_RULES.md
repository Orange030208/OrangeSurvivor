# 游戏开发专项规则

## 玩法数值通用规范

> 独立数值规范已抽取到 `GAMEPLAY_NUMERIC_GUIDELINES.md`。后续设计或修改玩法数值时，以该文档作为主要数值口径来源；本节保留现有摘要和敌人专项规则，便于架构规则集中查阅。

### 适用范围

- 设计或修改角色属性、武器属性、升级卡、饰品、Buff、敌人属性、商店经济、经验收益、掉落收益等玩法数值时，必须先查看本节。
- 涉及 `PropType`、`PropModifierData`、`PropModifierType`、`PropertiesManager` 或相关 ScriptableObject 配置时，默认遵循本节单位与结算口径。
- 新增属性或改变属性语义前，应先补充本文件中的单位定义，再改代码和资产。

### 百分比点口径

- 所有可配置的百分比数值，统一使用“百分比点”写法：`1` 表示 `1%`，`10` 表示 `10%`，`50` 表示 `50%`。
- 不要在配置、构造函数或 ScriptableObject 资产中写 `0.01` 表示 `1%`，也不要写 `0.5` 表示 `50%`。
- 运行时消费比例时，由消费代码或属性系统负责换算为 ratio：`ratio = value / 100f`。
- `BaseMultiplier`、`BonusMultiplier`、`FinalMultiplier` 也使用百分比点：`value: 50` 表示乘以 `1.5`，`value: -10` 表示乘以 `0.9`。
- `Add` 是否是百分比点取决于属性本身语义。例如 `CriticalChance +1` 是 `+1%`，但 `MoveSpeed +1` 是移动速度加 `1` 点。
- `Damage` 是全局最终伤害提升百分比点，`100` 表示最终伤害提升一倍；近战、远程、魔法、召唤攻击是点数型攻击池，武器上的使用区才使用百分比点。

#### 示例

最终移动速度变为 `1.5` 倍，应配置为：

- `propType: MoveSpeed`
- `modifierType: FinalMultiplier`
- `value: 50`

移动速度增加 `50` 点，应配置为：

- `propType: MoveSpeed`
- `modifierType: Add`
- `value: 50`

暴击率增加 `6%`，应配置为：

- `propType: CriticalChance`
- `modifierType: Add`
- `value: 6`

暴击伤害增加 `18%`，应配置为：

- `propType: CriticalPercent`
- `modifierType: Add`
- `value: 18`

### 属性单位定义

| 属性 | 配置单位 | 说明 |
| --- | --- | --- |
| `Attack` | 点数；通用成长改用 `Damage` | `Add +1` 表示每把武器每次命中基础伤害增加 1 点，会被攻速、投射物数量、穿透、多段命中和武器槽数量放大。升级卡、饰品、角色等通用成长默认不用 `Attack Add`，改用 `Damage` 百分比属性。 |
| `Damage` | 百分比点 | 全局最终伤害提升，`1` 表示最终伤害 `+1%`，`100` 表示最终伤害提升一倍。 |
| `MeleeAttack` | 点数 | 近战攻击池。武器按自身近战攻击使用区读取该属性，使用区 `20` 表示吃到该攻击池的 `20%`。 |
| `RangedAttack` | 点数 | 远程攻击池。武器按自身远程攻击使用区读取该属性，使用区 `20` 表示吃到该攻击池的 `20%`。 |
| `MagicAttack` | 点数 | 魔法攻击池。武器按自身魔法攻击使用区读取该属性，使用区 `20` 表示吃到该攻击池的 `20%`。 |
| `SummonAttack` | 点数 | 召唤攻击池。武器按自身召唤攻击使用区读取该属性，使用区 `20` 表示吃到该攻击池的 `20%`。 |
| `AttackSpeed` | 基础值为攻击频率；修饰值为百分比点 | 武器和敌人的基础 `AttackSpeed` 是每秒攻击次数。玩家、Buff、升级卡等修饰值写 `+10` 表示攻速 `+10%`。 |
| `CriticalChance` | 百分比点 | `1` 表示 `1%` 暴击率。 |
| `CriticalPercent` | 百分比点 | `180` 表示暴击伤害为 `180%`；作为修饰值时 `+18` 表示暴击伤害 `+18%`。 |
| `MoveSpeed` | 点数 | 玩家基础移速为 `100`；`+1` 表示增加 1 点移速。乘区使用百分比点。 |
| `MaxHealth` | 点数 | `+1` 表示增加 1 点最大生命。乘区使用百分比点。 |
| `DetectionRange` | 点数 | 检测距离点数；玩家武器索敌不使用它。 |
| `HealthRecoverySpeed` | 点数 | `+1` 表示每秒恢复 1 点生命。 |
| `Armor` | 点数 | 护甲减伤公式为 `Armor / (Armor + 100)`。 |
| `Luck` | 点数 | 掉落或经济概率相关的长期收益属性，`+1` 表示幸运增加 1 点。 |
| `Dodge` | 百分比点 | `1` 表示 `1%` 闪避率。 |
| `LifeSteal` | 百分比点 | `1` 表示 `1%` 生命偷取；当前规则是每打出 `1000` 最终伤害回复 `1` 点生命。 |
| `PickupRadius` | 点数 | 基础拾取半径为点数；乘区使用百分比点。 |
| `ProjectileCount` | 点数 | `+1` 表示增加 1 个投射物。 |
| `ProjectileSpeed` | 点数 | 基础弹速为点数；乘区使用百分比点。 |
| `AttackRange` | 点数 | 玩家武器实际使用的攻击距离属性；乘区使用百分比点。 |
| `ProjectilePierceCount` | 点数 | `+1` 表示增加 1 次穿透。 |
| `KnockbackStrength` | 点数 | 击退强度点数；默认 `10` 约等于击退 `1` 格/1 个世界单位，实际距离会受击退抗性、碰撞和最大速度限制影响；乘区使用百分比点。 |
| `KnockbackResistance` | 百分比点 | `1` 表示 `1%` 击退抗性；玩家不会被击退，该属性不作为玩家饰品、升级卡或角色成长属性投放。 |
| `ExperienceGain` | 百分比点 | `1` 表示 `+1%` 经验获取。 |
| `ShopPriceDiscount` | 百分比点 | `1` 表示 `1%` 商店折扣。 |
| `WaveGoldRewardBonus` | 点数 | `+1` 表示每波额外获得 1 金币。当前标准小额奖励为 `+5`。 |
| `DamageReduction` | 百分比点 | `1` 表示 `1%` 直接伤害减免；先算护甲，再算该减免。 |
| `HealingPower` | 百分比点 | `1` 表示 `+1%` 治疗效果；不影响生命偷取。 |
| `WeaponSlotCount` | 点数 | `+1` 表示增加 1 个武器槽，属于玩法级强属性。 |

### 数值预算模型

- 设计数值时，使用 `100` 预算作为一个普通升级卡主属性档位的参考，而不是严格公式。
- 低品质饰品应低于普通升级卡主属性档位，避免抢升级卡成长节奏。
- 机制效果需要额外消耗预算；如果机制改变构筑入口、投射物数量、武器槽位、经济雪球或生存兜底，应按高价值属性处理。
- 负面属性可以回收预算，但不能用来包装明显破坏平衡的强机制。
- 多个同类乘区或强线性属性叠加时，应提高后续投放成本，尤其是 `DamageReduction`、`Dodge`、`ProjectileCount`、`WeaponSlotCount`。

### 属性单位价值参考

| 属性 | 1 点口径 | 价值/点 | 普通档投放参考 | 设计备注 |
| --- | --- | ---: | --- | --- |
| `Attack` | 通用成长不再投放固定点数 | `Attack Add` 不作为常规预算项 | 仅保留给武器等级表、敌人基础攻击或明确限定效果 | 固定攻击点会被攻速、投射物、多段命中和武器槽数量放大；升级卡、饰品、角色等通用输出词条应使用 `Damage` 百分比。 |
| `Damage` | `+1%` 最终伤害 | 10 到 14 | `+6` 到 `+10` | 全局最终伤害提升，`100` 表示最终伤害乘以 `2`。 |
| `MeleeAttack` | `+1` 近战攻击池 | 待校准 | 首版只在有武器使用区后投放 | 武器使用区决定实际收益。 |
| `RangedAttack` | `+1` 远程攻击池 | 待校准 | 首版只在有武器使用区后投放 | 武器使用区决定实际收益。 |
| `MagicAttack` | `+1` 魔法攻击池 | 待校准 | 首版只在有武器使用区后投放 | 武器使用区决定实际收益。 |
| `SummonAttack` | `+1` 召唤攻击池 | 待校准 | 首版只在有武器使用区后投放 | 武器使用区决定实际收益。 |
| `AttackSpeed` | `+1%` 攻速修饰 | 10 | `+8` 到 `+12` | 武器和敌人的基础攻速仍是每秒攻击次数。 |
| `CriticalChance` | `+1%` 暴击率 | 16 | `+5` 到 `+7` | 价值受暴击伤害影响，后期可略微上调成本。 |
| `CriticalPercent` | `+1%` 暴击伤害 | 5 | `+15` 到 `+22` | 单独投放较温和，和暴击率绑定时要计组合价值。 |
| `MoveSpeed` | `+1` 移速，玩家基础移速为 `100` | 12 到 14 | `+6` 到 `+9` | `+1` 约等于基础移速的 `1%`。 |
| `MaxHealth` | `+1` 最大生命 | 3.5 | `+24` 到 `+32` | 稳定生存属性，适合低品质投放。 |
| `DetectionRange` | `+1` 检测范围 | 3 到 5 | `+15` 到 `+30` | 玩家武器索敌不使用它，玩家侧优先使用 `AttackRange`。 |
| `HealthRecoverySpeed` | 每秒 `+1` 回血 | 80 到 110 | `+0.5` 到 `+1` | 持续恢复会显著影响容错，低品质慎用大值。 |
| `Armor` | `+1` 护甲 | 30 到 40 | `+2` 到 `+4` | 低护甲时每点更值钱，后续边际减伤递减。 |
| `Luck` | `+1` 幸运 | 20 到 30 | `+3` 到 `+5` | 长期收益属性，和掉落/经济联动时要保守。 |
| `Dodge` | `+1%` 闪避 | 18 到 25 | `+4` 到 `+6` | 概率生存属性，叠高后体验和价值都会跳变。 |
| `LifeSteal` | `1%`，每造成 `1000` 最终伤害回复 `1` 生命 | 20 到 25 | `+3` 到 `+5` | 只依赖实际造成的最终伤害，不影响治疗效果。 |
| `PickupRadius` | `+1` 拾取半径点数，乘区为 `+1%` | 4 到 5 | `+20%` 到 `+30%` | 偏体验与收益稳定性，不应直接替代经验成长。 |
| `ProjectileCount` | `+1` 投射物 | 180 到 250 | 普通档不建议投放 | 玩法级强属性，优先交给高品质卡或带代价的传说饰品。 |
| `ProjectileSpeed` | `+1` 弹速点数，乘区为 `+1%` | 4 到 6 | `+15%` 到 `+25%` | 多数情况下是手感属性，和射程/命中率有隐性联动。 |
| `AttackRange` | `+1` 攻击距离点数，乘区为 `+1%` | 5 到 7 | `+12%` 到 `+20%` | 玩家武器实际使用的攻击距离属性。 |
| `ProjectilePierceCount` | `+1` 穿透 | 90 到 130 | 普通档慎用 | 清怪效率属性，和投射物数量、弹速、伤害联动很强。 |
| `KnockbackStrength` | `+1` 约等于 `0.1` 格击退预算 | 6 到 8 | `+8` 到 `+15` | 主要调整命中手感，常规枪械不建议轻易超过个位数基础值。 |
| `KnockbackResistance` | `+1%` 击退抗性 | 4 到 6 | 玩家不投放 | 玩家不会被击退，仅用于敌人或其他可被击退单位。 |
| `ExperienceGain` | `+1%` 经验获取 | 8 到 10 | `+10` 到 `+14` | 长期雪球属性，越早获得价值越高。 |
| `ShopPriceDiscount` | `+1%` 商店折扣 | 10 到 14 | `+6` 到 `+10` | 经济雪球属性，需注意折扣上限。 |
| `WaveGoldRewardBonus` | 每波 `+1` 金币 | 18 到 25 | `+5` | 当前标准小额奖励为 `+5`。 |
| `DamageReduction` | 护甲后 `+1%` 直接减伤 | 25 到 35 | `+2` 到 `+4` | 价值高于普通护甲点数，超过 `+5` 要谨慎。 |
| `HealingPower` | `+1%` 治疗效果 | 5 到 8 | `+12` 到 `+20` | 不影响生命偷取。 |
| `WeaponSlotCount` | `+1` 武器槽 | 250 到 400 | 普通档不投放 | 玩法级构筑入口，应只出现在高品质且通常需要代价。 |

### 饰品品质预算

| 品质 | 预算参考 | 属性与机制要求 |
| --- | ---: | --- |
| 普通 | 35 到 70 | 只给纯属性，优先单属性，不给特殊机制。 |
| 稀有 | 75 到 115 | 以属性为主，可以有少量双属性；特殊机制应非常轻。 |
| 史诗 | 130 到 180 | 可以有明确触发条件或小构筑倾向，但不改变核心玩法结构。 |
| 传说 | 200 到 280 | 可以提供构筑入口或强条件效果，但应有代价、限制或触发门槛。 |

### 配置与实现要求

- 百分比点属性作为直接属性加成时，优先使用 `PropModifierType.Add`。例如 `ExperienceGain +12%` 应写 `Add, value: 12`，不要写成乘区。
- 对已有点数属性做倍数变化时，使用 `BaseMultiplier`、`BonusMultiplier` 或 `FinalMultiplier`，并用百分比点写值。
- 不要为了表达 `+10%` 在资源中写 `0.1`。如果发现旧数据使用 ratio，应同步迁移到百分比点。
- 代码中消费概率、折扣、吸血、伤害减免等比例时，应明确调用 `PropValueUtility.PercentPointsToRatio` 或等价集中工具。
- 新增 UI 展示逻辑时，百分比点属性直接显示为 `%`；不要再乘以 `100`。
- 修改升级卡生成器时，必须同步修改已存在的升级卡资产，避免重建前后数值不一致。
- 修改武器暴击率、暴击伤害时，武器资产也使用百分比点：`criticalChance: 8` 表示 `8%`，`criticalPercent: 180` 表示 `180%`。
- 武器和敌人的基础 `AttackSpeed` 不按百分比点处理，它仍是攻击频率。只有通过 `PropModifierData` 施加的攻速修饰才按百分比点处理。

## 敌人设计

### 检测范围与攻击范围职责

- `DetectionRange` 用于敌人的感知、索敌、站位决策、攻击方式选择与状态切换等“是否发现目标、是否进入某个行为”的判定。
- `AttackRange` 用于敌人实际攻击是否能够命中、伤害区域大小、近战圆形攻击半径等“当前这次攻击能否打到目标”的判定。
- 不要把 `DetectionRange` 和 `AttackRange` 混为同一个语义来源；“发现目标”与“造成伤害”必须视为两个层次的问题。
- 远程攻击通常只需要 `DetectionRange` 参与攻击可释放判定；近战攻击通常既需要 `DetectionRange` 参与决策，又需要 `AttackRange` 参与实际命中判定。
- 当近战敌人存在多种攻击手段、冲锋/绕后/蓄力等行为分支时，应优先使用 `DetectionRange` 判断玩家位置和行为切换，再在具体攻击提交时使用 `AttackRange` 校验伤害范围。

### 攻击点位职责

- 敌人的近战点、射击点、激光点等攻击点位属于具体 Brain 与 Prefab 的显式依赖，应在对应 Brain 上声明 `Transform` 字段，由 Prefab 子物体拖引用。
- `EnemyAttackController` 不持有统一射击点；它只负责普通攻击槽、技能槽、攻击层与投射物发射等通用攻击控制职责。
- 攻击策略需要空间点位时，由 Brain 构造策略时直接传入对应 `Transform`；不要为点位再引入 `AttackConfig` 等包装结构，也不要把点位配置放进敌人 SO。
- 近战实际伤害区域默认是以近战点位为圆心、以 `AttackRange` 及攻击自身范围倍率为半径的圆形区域；不要再通过前向偏移类策略模拟攻击点。

### 普通攻击槽与技能槽

- 敌人攻击冷却必须区分普通攻击槽与技能槽：普通攻击槽用于近战、远程等基础攻击；技能槽用于激光、护盾、阶段技等显式技能。
- `AttackSpeed` 只影响普通攻击槽，不影响技能槽；技能冷却继续使用技能自身的显式冷却。
- 每一种普通攻击都应提供攻速收益系数，实际攻击间隔按 `1 / (AttackSpeed * attackSpeedBenefitRatio)` 计算。例如 `AttackSpeed = 1` 且收益系数为 `0.5` 时，该普通攻击间隔为 `2` 秒。
- 普通攻击槽应在策略初始化时注册，并在 `PropertiesManager` 的 `AttackSpeed` 变化事件中刷新间隔；禁止在每帧攻击判定中反复读取攻速。
- 普通攻击默认直接使用敌人 `Attack` 作为基础伤害，不在敌人 SO 上再配置 `damageMultiplier`、`cooldown`、`actionId` 或攻击音效包装。
- 普通攻击的 `actionId` 应作为代码常量写在对应敌人类型或行为代码中；攻击音效在具体攻击执行代码中播放，不作为敌人 SO 攻击配置。

### 攻击状态入场与提交

- 近战或远程普通攻击的范围检测首先用于“是否允许进入攻击状态/下达攻击指令”；一旦攻击状态已经入场，应播放完整攻击动画。
- 攻击提交点不得因为目标离开释放判定范围而取消动作或跳出状态；远程投射物仍应按提交时方向发出并消耗冷却。
- 攻击状态中目标临时离开范围时，敌人应停止移动并完成当前攻击动画；动画完成后再根据目标位置、冷却与优先级选择 Idle、追击、远程或其他技能状态。
- 近战、命中盒、范围扫掠或持续伤害区域等攻击，伤害结算必须按提交时的实际攻击区域查询可伤害目标；区域内没有合法目标时应表现为空挥，但动画、攻击挥出特效、音效与普通攻击冷却仍正常提交。

### 运行时属性修改

- 敌人运行时临时修改攻击范围、移动速度、攻击力、攻速等基础属性时，统一使用 `PropModifierData` 与 `PropertiesManager.AddModifiers/RemoveModifiers`。
- 不要在具体敌人行为脚本里直接改写基础属性、缓存属性结果，或额外传入临时倍率参数绕过属性系统。
- 可调的临时属性变化应放在敌人 ScriptableObject 中，例如 `List<PropModifierData>`，保持 Inspector 可配置、可调试、可复用。
- 临时属性修改必须使用稳定的 `sourceId`，状态进入时添加，状态退出时移除。
- `StopBrain`、`StartBrain`、`OnDisableComponent` 等中断入口必须清理可能残留的 Modifier。
- 同一状态可能重复进入或中断重入时，应避免重复 Add，并保证 Remove 幂等。

#### 示例

“攻击范围扩大 1.5 倍”应配置为：

- `propType: AttackRange`
- `modifierType: FinalMultiplier`
- `value: 50`

#### 例外

- 动画播放速度、状态计时、命中特效、Hit Shape 选择等不属于基础属性结算的数据，可以保留在行为状态或攻击定义中。
- 如果现有属性系统无法表达需求，应优先评估补充 `PropType`、`PropModifierType` 或属性结算扩展，而不是在具体玩法脚本中写死临时分支。
