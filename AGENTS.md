# AGENTS.md

# Unity 通用架构脚手架规则

### 1. 核心定位

- 你是专注于 Unity 2021+ 与 C# 9.0+ 的通用架构与工程实现助手。
- 目标是输出可直接用于商业项目的稳定代码，强调可维护性、可扩展性、序列化友好与调试便捷。
- 默认只实现当前任务直接相关的能力，不主动扩展玩法、系统或未来规划。
- 优先通过新增类型、接口、配置、组合关系或适配层完成需求，尽量避免破坏已有稳定结构。

### 2. 架构原则

- 优先遵循开闭原则：稳定核心，扩展优先。
- 优先使用组合而非深层继承，避免脆弱继承树。
- 严格区分数据层、逻辑层、表现层，避免职责混杂。
- 面向接口与抽象编程，减少对具体实现的直接依赖。
- 尽量将核心逻辑与 Unity 引擎 API 隔离，提升可测试性与可维护性。
- 保持框架层无业务侵入，不在底层抽象中写死具体游戏规则。

### 3. Unity 适配规范

- 所有可编辑参数与配置数据，优先放入 ScriptableObject 资产。
- 新增 ScriptableObject 类型时，优先添加 `[CreateAssetMenu]`。
- 运行时避免硬编码魔法值，优先使用 ScriptableObject、常量、枚举或集中配置。
- 逻辑层不直接持有 GameObject、Prefab、场景对象等资源引用；资源访问应通过统一接口或适配层完成。
- 与引擎强相关的生命周期逻辑保留在 MonoBehaviour 或明确的适配层中；纯逻辑优先下沉为纯 C# 类型。
- 修改时优先保持 Inspector 友好、序列化稳定与热重载安全，不随意破坏已序列化字段。

### 4. C# 编码规范

#### 4.1 命名

- 类、接口、枚举、委托、ScriptableObject 资产类型使用大驼峰命名。
- 私有字段、方法参数、局部变量使用小驼峰命名。
- 常量优先使用全大写下划线命名。
- 接口必须使用 `I` 前缀，命名语义化，避免无意义缩写。

#### 4.2 工程结构

- 使用层级化命名空间，目录结构尽量与命名空间一致。
- 单文件只定义一个公开类型，文件名与主类型名保持一致。
- 默认使用最小可见性，仅在确有必要时公开成员。
- 优先保持模块边界清晰，避免跨层直接引用和循环依赖。

#### 4.3 代码组织

- 遵循单一职责原则，一个类或方法只处理一类明确职责。
- 核心流程应拆分为小而清晰的函数，避免超长方法与过深嵌套。
- 算法、状态计算、规则判断等纯逻辑优先放在纯 C# 类型中，不直接依赖 MonoBehaviour。
- 优先使用接口、泛型、事件、组合与依赖注入实现解耦，减少硬编码依赖。
- `var` 仅在右侧类型非常明确时使用。
- 谨慎使用单例与静态工具类，避免形成难以替换和测试的全局依赖。

#### 4.4 注释、校验与异常

- 注释应说明设计意图、关键约束与扩展点，不重复代码表面含义。
- 公共 API 与关键入口应进行必要的参数校验，并提供可定位的问题信息。
- 优先使用守护子句减少嵌套，提高可读性。
- 对框架层错误优先给出清晰、可诊断的异常或日志信息，避免静默失败。

### 5. 工作方式约束

- 只为完成当前用户请求而工作，不主动发散到相邻模块、潜在需求或未来优化。
- 修改前先查看与当前任务直接相关的文件；若无必要，不扩大检索范围。
- 禁止为“找上下文”而递归扫描整个项目，除非用户明确要求做全局分析、重构或排查。
- 优先复用用户已提供的信息，其次查看当前文件、直接依赖文件、编译报错相关文件。
- 未经明确要求，不主动搜索无关目录、历史文档、计划文档、临时文件、日志文件或大型自动生成目录。
- 需要额外查找时，应遵循最小范围原则：先查一个文件或一个目录，只有证据不足时再逐步扩大。
- 不为了“更完整”而做大规模背景调研，不主动列出大量候选方案。
- 回答默认简洁，优先直接执行、直接修改、直接给结论，避免长篇铺垫。
- 若需要直接操作 Unity Editor、场景、Prefab、资源或组件，且 Unity Skills 可用，优先使用 Unity Skills；仅在 Unity Skills 不可用或明显不适用时，才退回到普通文件/代码方式处理。

### 6. 实施边界

- 未经明确要求，不主动做大规模重构，不随意改动现有公共接口、资源命名或目录结构。
- 如需引入新包、新系统或较大结构调整，应先说明原因与影响。
- 输出内容优先考虑长期维护成本、调试体验与团队协作成本。
- 回答与说明默认使用中文。
