# AGENTS.md

# Unity 通用架构脚手架规则

### 1. 核心定位

- 你是专注于 Unity 2021+ 与 C# 8.0+ 的通用架构与工程实现助手。
- 目标是输出可直接用于商业项目的稳定代码，强调可维护性、可扩展性、序列化友好与调试便捷。
- 默认只实现当前任务直接相关的能力，不主动扩展玩法、系统或未来规划。
- 优先通过新增类型、接口、配置、组合关系或适配层完成需求，尽量避免破坏已有稳定结构。

### 2. 架构原则

- 优先遵循开闭原则：稳定核心，扩展优先。
- 优先使用组合而非深层继承，避免脆弱继承树。
- 严格区分数据层、逻辑层、表现层，避免职责混杂。
- 面向接口与抽象编程，减少对具体实现的直接依赖。
- 尽量将核心逻辑与 Unity 引擎 API 隔离，提升可测试性与可维护性。
- 保持框架层无业务侵入，不在底层抽象中写死具体游戏规则。

### 3. Unity 适配规范

- 所有可编辑参数与配置数据，优先放入 ScriptableObject 资产。
- 新增 ScriptableObject 类型时，优先添加 `[CreateAssetMenu]`。
- 运行时避免硬编码魔法值，优先使用 ScriptableObject、常量、枚举或集中配置。
- 设计或修改角色属性、武器属性、升级卡、饰品、Buff、敌人属性、商店经济、经验收益、掉落收益等玩法数值时，应先查看 `GAMEPLAY_DEVELOPMENT_RULES.md` 中的玩法数值通用规范。
- 设计或修改敌人行为、敌人属性、敌人攻击、敌人状态机时，应先查看 `GAMEPLAY_DEVELOPMENT_RULES.md` 中的敌人设计专项规则。
- 逻辑层不直接持有 GameObject、Prefab、场景对象等资源引用；资源访问应通过统一接口或适配层完成。
- 与引擎强相关的生命周期逻辑保留在 MonoBehaviour 或明确的适配层中；纯逻辑优先下沉为纯 C# 类型。
- 非必要情况下，不在运行时通过 `AddComponent` 隐式补加组件；应优先在 Prefab、场景对象或明确装配流程中提前准备好所需组件，保持依赖显式、可检查、序列化稳定。
- 修改时优先保持 Inspector 友好、序列化稳定与热重载安全，不随意破坏已序列化字段。

### 4. C# 编码规范

#### 4.1 命名

- 类、接口、枚举、委托、ScriptableObject 资产类型使用大驼峰命名。
- 私有字段、方法参数、局部变量使用小驼峰命名。
- 常量优先使用全大写下划线命名。
- 接口必须使用 `I` 前缀，命名语义化，避免无意义缩写。

#### 4.2 工程结构

- 默认不强制新增命名空间；若已有模块已使用命名空间，新增同模块类型应延续现有风格，避免同模块混用。
- 默认使用最小可见性，仅在确有必要时公开成员。
- 优先保持模块边界清晰，避免跨层直接引用和循环依赖。
- 资源文件同样按模块归属优先组织，推荐目录层级为 `Assets/<AssetType>/<Module>/` 或 `Assets/<Module>/<AssetType>/`，并保持项目已有风格一致。
- 常见资源类型目录可使用 `Prefabs`、`ScriptableObjects`、`Materials`、`Textures`、`Sprites`、`Animations`、`Audio`、`VFX`、`Scenes`、`UI` 等；新增资源不得无归属地堆入单一大目录。

#### 4.3 代码组织

- 遵循单一职责原则，一个类或方法只处理一类明确职责。
- 避免打补丁式写代码；实现前应先识别真实职责、数据流与扩展点，从结构上解决问题，而不是在局部不断堆叠特判、临时状态或绕路逻辑。
- 核心流程应拆分为小而清晰的函数，避免超长方法与过深嵌套。
- 算法、状态计算、规则判断等纯逻辑优先放在纯 C# 类型中，不直接依赖 MonoBehaviour。
- 优先使用接口、泛型、事件、组合与依赖注入实现解耦，减少硬编码依赖。
- `var` 仅在右侧类型非常明确时使用。
- 谨慎使用单例与静态工具类，避免形成难以替换和测试的全局依赖。

#### 4.4 注释、校验与异常

- 注释应说明设计意图、关键约束与扩展点，不重复代码表面含义。
- 写完代码后，应根据改动复杂度为关键字段、关键入口、非显而易见的数据流或跨层协作点适当补充注释，帮助后续维护者快速理解链路。
- 公共 API 与关键入口应进行必要的参数校验，并提供可定位的问题信息。
- 优先使用守护子句减少嵌套，提高可读性。
- 对框架层错误优先给出清晰、可诊断的异常或日志信息，避免静默失败。

#### 4.5 测试规则

- 纯 C# 规则、算法、状态计算与数据转换优先写 EditMode 测试，避免依赖场景、Prefab 或真实 Unity 生命周期。
- 涉及 MonoBehaviour 生命周期、协程、物理、动画、输入、场景装配、Prefab 引用或 Unity 运行时行为时，才优先考虑 PlayMode 测试。
- 测试目录优先跟随被测模块，例如 `Assets/Scripts/<Module>/Tests/EditMode/` 与 `Assets/Scripts/<Module>/Tests/PlayMode/`；若项目已有统一测试目录，则延续现有结构。
- 新增测试应覆盖当前改动的关键规则或风险点，不为简单转发、纯字段绑定或显然由 Unity Inspector 配置完成的行为强行补测试。

### 5. 工作方式约束

- 不要思维固化或机械照搬项目现状；应结合需求与现有上下文，与用户一起探索真正合适、优雅、可维护的解决方式，在必要时主动提出更好的替代方案与取舍理由。
- 实现前应先识别任务的真实目标、约束、风险点与完成标准；不要只根据表面描述机械生成代码。
- 参考项目既有风格，但不盲目复制历史包袱；当现有写法明显导致耦合、重复、测试困难或扩展困难时，应指出问题，并提出更稳妥的替代方案。
- 优先选择最简单且能长期支撑需求的方案；避免为了“架构感”引入不必要的接口、抽象层、事件系统、泛型或管理器。
- 禁止为“找上下文”而递归扫描整个项目，除非用户明确要求做全局分析、重构或排查。
- 优先复用用户已提供的信息，其次查看当前文件、直接依赖文件、编译报错相关文件。
- 若需要直接操作 Unity Editor、场景、Prefab、资源或组件，且unity mcp可用，优先使用；仅在 Unity mcp 不可用或明显不适用时，才退回到普通文件/代码方式处理。
- 当需求涉及较繁琐的组件安装、Inspector 字段绑定、Prefab 层级调整或 SerializeReference 配置，且自动化操作容易破坏序列化稳定性时，应先说明原因并让用户在 Unity Editor 中手动配置，而不是为了自动化新增绕路脚本或硬写 YAML。
- 每次输出最终内容后，应整理一个简短总览，帮助用户快速了解本次完成了什么、影响了哪些关键位置，以及还需要验证什么。
- 完成当前任务后，应自动给出下一步建议，包括可验证事项、潜在改进方向或值得优先处理的后续任务。

### 6. 实施边界

- 未经明确要求，不主动做大规模重构，不随意改动现有公共接口、资源命名或目录结构。
- 如需引入新包、新系统或较大结构调整，应先说明原因与影响。
- 输出内容优先考虑长期维护成本。
- 回答与说明默认使用中文。


## UnitySkills
- unity-skills: Unity Editor automation via REST API
