# 玩法数值规范

本文档是项目后续配置角色、武器、Buff、敌人、经济、经验和掉落时的独立数值口径文档。

## 1. 文档状态

- `硬规则`：已经由代码结算方式、资产格式或现有规则确认，修改前必须同步评估代码和资产。
- `预算建议`：用于设计、审查和初调，不是不可突破的公式；突破时必须有机制限制、生效条件或明确代价。
- `待校准`：当前项目已有资产暴露出风险，但还需要结合实机体验、关卡节奏和设计目标再定最终档位。

## 2. 通用单位口径

### 2.1 百分比点

所有可配置的百分比数值统一使用百分比点：

- `1` 表示 `1%`
- `10` 表示 `10%`
- `50` 表示 `50%`

禁止在配置和资产中写 `0.01` 表示 `1%`，也禁止写 `0.5` 表示 `50%`。运行时需要 ratio 时，由消费代码统一换算为 `value / 100f`。

### 2.2 距离点

距离相关属性统一使用距离点：

- `100` 表示 `1` 格距离，也等价于 Unity 世界空间中的 `1` 个距离单位。
- `MoveSpeed 100` 表示每秒移动 `1` 格。
- `AttackRange 100` 表示攻击范围 `1` 格。
- `DetectionRange 100` 表示检测范围 `1` 格。

配置和资产中不再直接写 `1` 表示 `1` 格距离。运行时需要物理距离或速度时，由消费代码统一换算为 `value / 100f`。

### 2.3 乘区

`BaseMultiplier`、`BonusMultiplier`、`FinalMultiplier` 也使用百分比点：

- `value: 50` 表示乘以 `1.5`
- `value: -10` 表示乘以 `0.9`

属性最终值按下面顺序计算：

```text
baseAfterBaseMultiplier = base * (1 + BaseMultiplier)
result = (baseAfterBaseMultiplier + Add) * (1 + BonusMultiplier)
final = result * (1 + FinalMultiplier)
```

`AttackSpeed` 是特殊项：通过 `PropModifierData.Add` 配置时也作为百分比点处理，等价进入加成乘区。

### 2.4 Add 的单位

`Add` 的单位取决于属性语义：

- `CriticalChance +6` 是暴击率 `+6%`
- `ExperienceGain +12` 是经验获取 `+12%`
- `MoveSpeed +8` 是移动速度 `+8` 距离点/秒
- `HealthRecoverySpeed +10` 是每秒恢复 `+1` 点生命
- `Attack Add +1` 是固定攻击点，不是全局 DPS `+1`；玩家通用词条不要使用该写法，应使用 `Damage` 百分比属性。
- `Damage +10` 是最终伤害提升 `+10%`；`100` 表示最终伤害提升一倍。
- `MeleeAttack +10` 等四类攻击属性是类型攻击池点数，不是百分比乘区；只有武器上的对应使用区按百分比点换算。

## 3. 价值定义

`价值` 是项目内部数值预算单位，用 `BP`（Budget Point）表示。它用于横向比较不同属性的强度成本，不是游戏内金币、伤害、概率、权重或 UI 显示值。

基础锚点：

```text
100 BP = 一条标准普通主属性档
```

`价值/点` 表示每增加 1 个配置单位需要消耗的 BP：

```text
属性预算 = 属性变化量 * 价值/点
```

例如：

```text
Damage +5% = 5 * 20 = 100 BP
AttackSpeed +5% = 5 * 20 = 100 BP
MaxHealth +10 = 10 * 10 = 100 BP
Armor +1 = 1 * 100 = 100 BP
CriticalChance +2% = 2 * 50 = 100 BP
Dodge +3% = 3 * 33 = 99 BP
```

表中的 `价值/点` 默认描述永久、无条件、立即生效、无负面代价、无冷却、常规覆盖率的基础裸值。

条件型、短时型或低覆盖率效果应先计算属性预算，再按覆盖率修正：

```text
有效预算 = 属性预算 * 时间覆盖率 * 触发可靠性 * 目标覆盖率
```

机制型效果不能只按线性属性相加。新增武器、升级武器、增加投射物、增加武器槽、无敌、复活、强制暴击、经济雪球等效果，应使用独立机制预算或显著更高的 `价值/点`。

## 4. 属性单位表

本节只定义属性的数值单位、配置含义和基础价值口径；预算评估、平衡结论或具体系统实现细节分别维护在后续专项章节中。

属性值本身不做强制截断。`PropertiesManager` 和属性面板应保留真实累计值，例如玩家拥有 `Dodge 80` 时属性仍显示为 `80%`。运行时有效上限集中在 `PropValueUtility` 中维护，消费属性时统一通过该入口换算，不在属性累加阶段截断。

| 属性 | 配置单位 | 说明 | 运行时有效上限 | 价值/点 |
| --- | --- | --- | --- | ---: |
| `Attack` | 点数 | `Add +1` 表示基础攻击增加 1 点；它会被每把武器的攻速、投射物数量、穿透、多段命中和暴击放大，因此通用攻击成长默认改用 `Damage` 百分比，`Attack Add` 只保留给武器自身等级表、敌人基础攻击或明确限定的单把武器/单次攻击模板。 | - | 25 |
| `Damage` | 百分比点 | 全局最终伤害提升，`1` 表示最终伤害 `+1%`，`100` 表示最终伤害提升一倍。 | - | 20 |
| `MeleeAttack` | 点数 | `Add +1` 表示近战攻击增加 1 点。 | - | 10 |
| `RangedAttack` | 点数 | `Add +1` 表示远程攻击增加 1 点。 | - | 10 |
| `MagicAttack` | 点数 | `Add +1` 表示魔法攻击增加 1 点。 | - | 10 |
| `SummonAttack` | 点数 | `Add +1` 表示召唤攻击增加 1 点。 | - | 10 |
| `AttackSpeed` | 基础值为每秒攻击次数；修饰值为百分比点 | `+10` 表示攻速 `+10%`；攻击间隔为 `1 / finalAttackSpeed`。 | - | 20 |
| `CriticalChance` | 百分比点 | `1` 表示 `1%` 暴击率。 | 暴击判定最高 `100%` | 50 |
| `CriticalPercent` | 百分比点 | `180` 表示暴击伤害 `180%`；修饰 `+18` 表示暴击伤害 `+18%`。 | - | 12 |
| `MoveSpeed` | 距离点/秒 | `100` 表示每秒移动 1 格；`Add +1` 表示移动速度增加 0.01 格/秒。 | - | 20 |
| `MaxHealth` | 点数 | `+1` 表示增加 1 点最大生命。 | - | 10 |
| `DetectionRange` | 距离点 | `100` 表示 1 格检测距离；`Add +1` 表示检测距离增加 0.01 格。 | - | - |
| `HealthRecoverySpeed` | 回复点 | `10` 表示每秒恢复 1 点生命；`Add +1` 表示每秒恢复 0.1 点生命。 | - | 5 |
| `Armor` | 点数 | `+1` 表示护甲增加 1 点；减伤公式为 `armor / (abs(armor) + 25)`。 | 单段护甲减伤最高 `95%` | 100 |
| `Luck` | 点数 | `+1` 表示幸运增加 1 点。 | - | 20 |
| `Dodge` | 百分比点 | `1` 表示 `1%` 闪避率。 | 闪避判定最高 `50%` | 33 |
| `LifeSteal` | 百分比点 | `1` 表示 `1%` 生命偷取。 | - | 50 |
| `PickupRadius` | 点数 | `Add +1` 表示拾取半径增加 1 点；乘区使用百分比点。 | - | 5 |
| `ProjectileCount` | 点数 | `+1` 表示增加 1 个投射物。 | - | 300 |
| `ProjectileSpeed` | 点数 | `Add +1` 表示投射物速度增加 1 点；乘区使用百分比点。 | - | 5 |
| `AttackRange` | 距离点 | `100` 表示 1 格攻击距离；`Add +1` 表示攻击距离增加 0.01 格；乘区使用百分比点。 | - | 10 |
| `ProjectilePierceCount` | 点数 | `+1` 表示增加 1 次穿透。 | - | 200 |
| `KnockbackStrength` | 点数 | `+1` 表示击退强度增加 1 点；乘区使用百分比点。 | - | 5 |
| `KnockbackResistance` | 百分比点 | `1` 表示 `1%` 击退抗性。 | 击退强度折减最高 `100%` | 5 |
| `ExperienceGain` | 百分比点 | `1` 表示经验获取 `+1%`。 | - | 15 |
| `ShopPriceDiscount` | 百分比点 | `1` 表示商店折扣 `1%`。 | 折扣最高 `50%`，最终价格倍率不低于 `50%` | 15 |
| `WaveGoldRewardBonus` | 点数 | `+1` 表示每波额外获得 1 金币。 | - | 12.5 |
| `DamageReduction` | 百分比点 | `1` 表示直接减伤 `1%`。 | 直接减伤最高 `50%` | 35 |
| `HealingPower` | 百分比点 | `1` 表示治疗效果 `+1%`。 | - | 10 |
| `WeaponSlotCount` | 点数 | `+1` 表示增加 1 个武器槽。 | - | 600 |
