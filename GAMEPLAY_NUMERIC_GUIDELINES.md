# 玩法数值规范

本文档是项目后续配置角色、武器、升级卡、饰品、Buff、敌人、经济、经验和掉落时的独立数值口径文档。

## 1. 文档状态

- `硬规则`：已经由代码结算方式、资产格式或现有规则确认，修改前必须同步评估代码和资产。
- `预算建议`：用于设计、审查和初调，不是不可突破的公式；突破时必须有机制限制、投放限制或明确代价。
- `待校准`：当前项目已有资产暴露出风险，但还需要结合实机体验、关卡节奏和设计目标再定最终档位。

## 2. 已抽取的现有规则来源

### 2.1 项目约束

- `AGENTS.md` 要求修改角色属性、武器属性、升级卡、饰品、Buff、敌人属性、商店经济、经验收益、掉落收益等玩法数值前，先查看玩法数值规范。
- `GAMEPLAY_DEVELOPMENT_RULES.md` 已有百分比点口径、属性单位、预算模型、饰品品质预算和敌人行为数值职责。
- 本文档从上述内容中抽取数值规范，后续新增或调整数值规则时优先维护本文档。

### 2.2 代码确认的结算口径

- `PropModifierData` / `PropValueUtility`：百分比点统一通过 `value / 100f` 转为 ratio；`Damage` 是全局最终伤害提升百分比点。
- `PropertiesManager`：属性最终值按 `BaseMultiplier`、`Add`、`BonusMultiplier`、`FinalMultiplier` 顺序结算；旧玩家武器公式中的 `Attack Add` 会被攻速、投射物数量、穿透、多段命中和武器槽数量放大，因此当前通用输出成长应使用 `Damage` 百分比属性。
- `PropDescriptionUtility`：百分比属性和乘区直接显示 `%`，不再额外乘 `100`。
- `WeaponDataSO` / `Weapon`：武器等级属性中的 `criticalChance`、`criticalPercent` 使用百分比点；武器基础 `AttackSpeed` 是每秒攻击次数；武器可配置近战、远程、魔法、召唤攻击使用区，使用区也是百分比点。
- `HitStartModifier` / `DAMAGE_FLOW.md`：闪避、护甲、直接减伤、暴击和最终伤害的结算位置已统一。
- `HealthComponent`：生命偷取按最终造成伤害计算，`1%` 生命偷取等于每造成 `1000` 最终伤害回复 `1` 点生命。
- `PlayerLevelConfigSO` / `PlayerLevel`：经验需求公式已配置化，经验加成使用百分比点。
- `ShopManager`：商店折扣最大按 `80%` 计算，最终价格倍率不低于 `20%`；刷新费用从 `5` 开始，每次付费刷新 `+1`。
- `WeaponLevelHelper` / `WeaponPriceHelper`：武器等级范围为 `1` 到 `4`，商店价格按每级 `1.5` 倍缩放。
- `Coin`：当前金币掉落物被拾取时给予 `1` 经验和 `1` 金币。
- `GameContentMigrationUtility` / 内容池资产：当前掉落池基准为宝箱 `0.1`、金币 `0.9`，宝箱权重受幸运影响。
- `UpgradeCardSystemBuilder` / 升级卡池：当前升级卡投放有稀有度、基础权重、最大拾取次数、互斥、前置标签、最小波次和动态权重规则。

## 3. 通用单位口径

### 3.1 百分比点

所有可配置的百分比数值统一使用百分比点：

- `1` 表示 `1%`
- `10` 表示 `10%`
- `50` 表示 `50%`

禁止在配置和资产中写 `0.01` 表示 `1%`，也禁止写 `0.5` 表示 `50%`。运行时需要 ratio 时，由消费代码统一换算为 `value / 100f`。

### 3.2 乘区

`BaseMultiplier`、`BonusMultiplier`、`FinalMultiplier` 也使用百分比点：

- `value: 50` 表示乘以 `1.5`
- `value: -10` 表示乘以 `0.9`

属性最终值按下面顺序计算：

```text
baseAfterBaseMultiplier = base * (1 + BaseMultiplier)
result = (baseAfterBaseMultiplier + Add) * (1 + BonusMultiplier)
final = result * (1 + FinalMultiplier)
```

`AttackSpeed` 是特殊项：通过 `PropModifierData.Add` 配置时也作为百分比点处理，等价进入加成乘区。

### 3.3 Add 的单位

`Add` 的单位取决于属性语义：

- `CriticalChance +6` 是暴击率 `+6%`
- `ExperienceGain +12` 是经验获取 `+12%`
- `MoveSpeed +8` 是移动速度 `+8` 点
- `Attack Add +1` 是固定攻击点，不是全局 DPS `+1`；玩家通用词条不要使用该写法，应使用 `Damage` 百分比属性。
- `Damage +10` 是最终伤害提升 `+10%`；`100` 表示最终伤害提升一倍。
- `MeleeAttack +10` 等四类攻击属性是类型攻击池点数，不是百分比乘区；只有武器上的对应使用区按百分比点换算。

## 4. 属性单位表

| 属性 | 配置单位 | 说明 |
| --- | --- | --- |
| `Attack` | 点数；通用成长改用 `Damage` | `Add +1` 表示每把武器每次命中基础伤害增加 1 点，会被攻速、投射物数量、穿透、多段命中和武器槽数量放大。升级卡、饰品、角色等通用成长默认不用 `Attack Add`，改用 `Damage` 百分比属性。 |
| `Damage` | 百分比点 | 全局最终伤害提升，`1` 表示最终伤害 `+1%`，`100` 表示最终伤害提升一倍。 |
| `MeleeAttack` | 点数 | 近战攻击池。武器按自身近战攻击使用区读取该属性，使用区 `20` 表示吃到该攻击池的 `20%`。 |
| `RangedAttack` | 点数 | 远程攻击池。武器按自身远程攻击使用区读取该属性，使用区 `20` 表示吃到该攻击池的 `20%`。 |
| `MagicAttack` | 点数 | 魔法攻击池。武器按自身魔法攻击使用区读取该属性，使用区 `20` 表示吃到该攻击池的 `20%`。 |
| `SummonAttack` | 点数 | 召唤攻击池。武器按自身召唤攻击使用区读取该属性，使用区 `20` 表示吃到该攻击池的 `20%`。 |
| `AttackSpeed` | 武器/敌人基础值为每秒攻击次数；修饰值为百分比点 | `+10` 表示攻速 `+10%`。 |
| `CriticalChance` | 百分比点 | `1` 表示 `1%` 暴击率。 |
| `CriticalPercent` | 百分比点 | 武器基础 `180` 表示暴击伤害 `180%`；修饰 `+18` 表示暴击伤害 `+18%`。 |
| `MoveSpeed` | 点数 | 玩家基础移速当前为 `160`；设计参考仍按 `+1` 约等于基础移速约 `0.6%` 到 `1%` 估算。 |
| `MaxHealth` | 点数 | `+1` 表示增加 1 点最大生命。 |
| `DetectionRange` | 点数 | 敌人感知、索敌、行为切换用，不作为玩家武器索敌距离。 |
| `HealthRecoverySpeed` | 点数 | `+1` 表示每秒恢复 1 点生命。 |
| `Armor` | 点数 | 护甲减伤公式见战斗结算。 |
| `Luck` | 点数 | 掉落或经济概率相关的长期收益属性。 |
| `Dodge` | 百分比点 | `1` 表示 `1%` 闪避率。 |
| `LifeSteal` | 百分比点 | `1` 表示 `1%` 生命偷取；当前等于每 `1000` 最终伤害回复 `1` 生命。 |
| `PickupRadius` | 点数 | 基础拾取半径为点数；乘区使用百分比点。 |
| `ProjectileCount` | 点数 | `+1` 表示增加 1 个投射物。 |
| `ProjectileSpeed` | 点数 | 基础弹速为点数；乘区使用百分比点。 |
| `AttackRange` | 点数 | 玩家武器实际攻击距离属性；乘区使用百分比点。 |
| `ProjectilePierceCount` | 点数 | `+1` 表示增加 1 次穿透。 |
| `KnockbackStrength` | 点数 | 默认 `10` 约等于击退 `1` 格/1 个世界单位；乘区使用百分比点。 |
| `KnockbackResistance` | 百分比点 | `1` 表示 `1%` 击退抗性；玩家通常不投放。 |
| `ExperienceGain` | 百分比点 | `1` 表示经验获取 `+1%`。 |
| `ShopPriceDiscount` | 百分比点 | `1` 表示商店折扣 `1%`。 |
| `WaveGoldRewardBonus` | 点数 | `+1` 表示每波额外获得 1 金币；当前消费链路待确认。 |
| `DamageReduction` | 百分比点 | 护甲后直接减伤；`1` 表示 `1%`。 |
| `HealingPower` | 百分比点 | `1` 表示治疗效果 `+1%`；不影响生命偷取。 |
| `WeaponSlotCount` | 点数 | `+1` 表示增加 1 个武器槽，属于玩法级强属性。 |

## 5. 属性价值参考

该表用于粗估预算。它不是精确公式，尤其在同类属性叠高、机制组合或早期经济收益时要提高成本。

| 属性 | 1 点口径 | 价值/点 | 普通升级卡主属性参考 |
| --- | --- | ---: | --- |
| `Attack` | 通用成长不再投放固定点数 | `Attack Add` 不作为常规预算项 | 仅保留给武器等级表、敌人基础攻击或明确限定效果 |
| `Damage` | `+1%` 最终伤害 | 10 到 14 | 普通档建议 `+6` 到 `+10` |
| `MeleeAttack` | `+1` 近战攻击池 | 待校准 | 首版只在有武器使用区后投放 |
| `RangedAttack` | `+1` 远程攻击池 | 待校准 | 首版只在有武器使用区后投放 |
| `MagicAttack` | `+1` 魔法攻击池 | 待校准 | 首版只在有武器使用区后投放 |
| `SummonAttack` | `+1` 召唤攻击池 | 待校准 | 首版只在有武器使用区后投放 |
| `AttackSpeed` | `+1%` 攻速修饰 | 10 | `+8` 到 `+12` |
| `CriticalChance` | `+1%` 暴击率 | 16 | `+5` 到 `+7` |
| `CriticalPercent` | `+1%` 暴击伤害 | 5 | `+15` 到 `+22` |
| `MoveSpeed` | `+1` 移速 | 12 到 14 | `+6` 到 `+9` |
| `MaxHealth` | `+1` 最大生命 | 3.5 | `+24` 到 `+32` |
| `DetectionRange` | `+1` 检测范围 | 3 到 5 | `+15` 到 `+30` |
| `HealthRecoverySpeed` | 每秒 `+1` 回血 | 80 到 110 | `+0.5` 到 `+1` |
| `Armor` | `+1` 护甲 | 30 到 40 | `+2` 到 `+4` |
| `Luck` | `+1` 幸运 | 20 到 30 | `+3` 到 `+5` |
| `Dodge` | `+1%` 闪避 | 18 到 25 | `+4` 到 `+6` |
| `LifeSteal` | `+1%` 生命偷取 | 20 到 25 | `+3` 到 `+5` |
| `PickupRadius` | `+1` 拾取半径或 `+1%` 乘区 | 4 到 5 | `+20%` 到 `+30%` |
| `ProjectileCount` | `+1` 投射物 | 180 到 250 | 普通档不投放 |
| `ProjectileSpeed` | `+1` 弹速或 `+1%` 乘区 | 4 到 6 | `+15%` 到 `+25%` |
| `AttackRange` | `+1` 攻击距离或 `+1%` 乘区 | 5 到 7 | `+12%` 到 `+20%` |
| `ProjectilePierceCount` | `+1` 穿透 | 90 到 130 | 普通档慎用 |
| `KnockbackStrength` | `+1` 击退强度 | 6 到 8 | `+8` 到 `+15` |
| `KnockbackResistance` | `+1%` 击退抗性 | 4 到 6 | 玩家不投放 |
| `ExperienceGain` | `+1%` 经验获取 | 8 到 10 | `+10` 到 `+14` |
| `ShopPriceDiscount` | `+1%` 商店折扣 | 10 到 14 | `+6` 到 `+10` |
| `WaveGoldRewardBonus` | 每波 `+1` 金币 | 18 到 25 | `+5` |
| `DamageReduction` | `+1%` 直接减伤 | 25 到 35 | `+2` 到 `+4` |
| `HealingPower` | `+1%` 治疗效果 | 5 到 8 | `+12` 到 `+20` |
| `WeaponSlotCount` | `+1` 武器槽 | 250 到 400 | 普通档不投放 |

### 5.1 攻击点数专项

`Attack Add` 是最容易误估的属性。旧武器公式下它不是“角色总攻击力 +1”，而是加到每把武器的基础伤害上，再参与每把武器的攻速、投射物、穿透、多段命中、暴击和后续乘区。

因此首版规则直接收紧为：升级卡、饰品、角色天赋、Buff 等通用攻击成长默认不使用 `Attack Add`，统一使用 `Damage` 百分比属性。

对单把武器，粗略收益可以先按下面估算：

```text
perSecondGain ~= AttackAdd
              * attackSpeed
              * averageProjectileOrHitCount
              * averageHitTargetsPerAttack
              * averageCriticalMultiplier
```

如果玩家有多把武器，该收益还会对每把武器分别生效。因此武器槽越多，固定攻击点数越接近“全武器平铺增伤”，而不是单一武器成长。

当前武器库里已经存在高攻速例子：

- `AK47` 1 级攻速为 `4`，`Attack +1` 至少约等于每秒 `+4` 基础伤害，且还没计算暴击、投射物和穿透。
- `MiniUzi` 1 级攻速为 `2`，基础攻击为 `6`，`Attack +1` 等价单发基础伤害约 `+16.7%`。
- `SniperRifle` 1 级攻速为 `0.38`，基础攻击为 `38`，`Attack +1` 等价单发基础伤害约 `+2.6%`。

首版设计规则：

- 通用升级卡、饰品、角色天赋、Buff 默认使用 `Damage Add`，不使用 `Attack Add`。
- 可重复普通输出成长优先使用 `Damage +6%` 到 `+10%`。
- 稀有或带条件输出成长可以使用 `Damage +10%` 到 `+15%`。
- 史诗以上爆发或玻璃大炮类可以使用更高 `Damage`，但必须有拾取次数限制、前置条件、互斥或负面代价。
- `Attack Add` 仅保留给武器自身等级表、敌人基础攻击、或明确限定到单把武器/单次攻击模板的特殊效果；不得作为全局通用词条。
- 审查旧资产时，遇到通用 `Attack Add`，优先迁移为 `Damage` 百分比，而不是继续调整固定点数大小。

## 6. 战斗结算规则

### 6.1 武器与玩家属性合成

武器自身提供基础攻击、基础攻速、基础暴击率、基础暴击伤害、基础攻击范围和击退强度。玩家属性通过 `PropertiesManager.GetPropValueWithAdditionalBase` 参与同一套属性修饰。

- 最终武器面板伤害：武器攻击力先加上类型攻击贡献，再乘全局 `Damage`。
- 最终攻速：武器 `AttackSpeed` 是每秒攻击次数，吃玩家攻速修饰，最终攻击间隔为 `1 / finalAttackSpeed`。
- 最终暴击率：武器暴击率 + 玩家暴击率，最后 clamp 到 `0%` 到 `100%`。
- 最终暴击倍率：武器暴击伤害 + 玩家暴击伤害加成，最低 `100%`。
- 最终攻击范围：武器范围作为额外基础值，吃玩家 `AttackRange` 修饰。
- 最终击退强度：武器击退作为额外基础值，吃玩家 `KnockbackStrength` 修饰。

武器伤害公式：

```text
typedAttack = MeleeAttack * meleeUsage / 100
            + RangedAttack * rangedUsage / 100
            + MagicAttack * magicUsage / 100
            + SummonAttack * summonUsage / 100

weaponDamage = (weaponAttack + typedAttack) * (1 + Damage / 100)
```

其中 `meleeUsage`、`rangedUsage`、`magicUsage`、`summonUsage` 是武器自身配置的攻击类型使用区，统一使用百分比点。`Damage` 是玩家最终伤害提升属性，`Damage = 100` 表示最终武器伤害乘以 `2`。

### 6.2 命中结算顺序

当前统一伤害链路：

1. 构造 `HitRequest` 和 `HitSpec`。
2. `HitStartModifier` 读取目标闪避、护甲、直接减伤、击退抗性。
3. 判定暴击和闪避。
4. `HitResolver` 收口最终伤害。
5. `HealthComponent` 应用 `HitResult`。

最终伤害核心规则：

```text
if IsCancelled: FinalDamage = 0
else if IsDodged: FinalDamage = 0
else:
    damage = Damage
    if IsCritical: damage *= CritMultiplier
    damage *= (1 - DamageReduction)
```

### 6.3 护甲和直接减伤

护甲减伤：

```text
armorReduction = armor / (armor + 100)
```

- 护甲最低按 `-95` 参与计算，避免公式极端值。
- 护甲穿透先降低正护甲，再计算护甲减伤。
- 护甲减伤和 `DamageReduction` 是顺序减伤：

```text
combinedReduction = 1 - (1 - armorReduction) * (1 - damageReduction)
```

- 单段减伤和最终合并减伤都不应超过 `95%`。

### 6.4 闪避、击退抗性、生命偷取

- `Dodge` 按百分比点换算，运行时 clamp 到 `0%` 到 `100%`。
- `KnockbackResistance` 按百分比点换算，运行时 clamp 到 `0%` 到 `100%`。
- `LifeSteal` 按最终造成伤害计算：

```text
heal = finalDamage * (LifeSteal / 100) * 0.1
```

因此 `LifeSteal = 1` 时，每造成 `1000` 最终伤害回复 `1` 点生命。

## 7. 成长、经济和掉落

### 7.1 玩家等级

当前经验需求公式：

```text
levelOffset = max(1, level) - 1
requiredXp = baseRequiredExperience
           + requiredExperiencePerLevel * levelOffset
           + requiredExperienceGrowthPerLevel * levelOffset * max(0, levelOffset - 1) / 2
requiredXp = max(minimumRequiredExperience, requiredXp)
```

当前配置：

| 字段 | 当前值 |
| --- | ---: |
| 起始等级 | 1 |
| 起始经验 | 0 |
| 基础升级经验 | 5 |
| 每级线性增量 | 5 |
| 每级增长项 | 0 |
| 每级升级点 | 1 |

经验获取加成使用百分比点，最终经验会保留小数累积，按 floor 发放整数经验。

### 7.2 金币与商店

- 当前 `Coin` 掉落物给予 `1` 经验和 `1` 金币。
- 商店默认刷新费用为 `5`，每次付费刷新后下一次费用 `+1`。
- 免费刷新优先消耗，不增加付费刷新费用。
- `ShopPriceDiscount` 最大按 `80%` 生效，最终价格倍率不低于 `20%`。
- 武器价格按等级缩放：`price = basePrice * 1.5^(level - 1)`，最后四舍五入。

### 7.3 掉落池

当前掉落池基准权重：

| 掉落 | 基础权重 |
| --- | ---: |
| 金币 | 0.9 |
| 宝箱 | 0.1 |

宝箱当前有幸运权重规则：

```text
chestWeightMultiplier = clamp(1 + Luck * 0.02, 0, 5)
```

注意：这是权重乘区，不是最终概率。最终概率还取决于同池其他候选项。

### 7.4 波次金币奖励

`WaveGoldRewardBonus` 的规则语义是“每波额外获得金币”。当前代码里可以通过经济卡写入该属性，但尚未确认全局波次结算中存在稳定消费点。

在消费链路明确前：

- 不要把 `WaveGoldRewardBonus` 放进角色基础属性。
- 不要把大额 `WaveGoldRewardBonus` 当作初始金币或关卡奖励。
- 若要启用，应先补齐波次结算消费逻辑、上限和 UI 展示。

## 8. 武器数值规范

### 8.1 等级与价格

- 武器等级固定为 `1` 到 `4`。
- 等级越界统一 clamp。
- 商店价格每升一级乘以 `1.5`。

### 8.2 武器基础属性

武器等级表字段：

| 字段 | 口径 |
| --- | --- |
| `attack` | 基础攻击力，点数。 |
| `attackSpeed` | 每秒攻击次数，最低 `0.01`。 |
| `criticalChance` | 百分比点，`8` 表示 `8%`。 |
| `criticalPercent` | 百分比点，最低 `100`。 |
| `range` | 攻击范围点数。 |
| `knockbackStrength` | 击退强度点数。 |

武器伤害类型使用区字段：

| 字段 | 口径 |
| --- | --- |
| `meleeAttackUsagePercent` | 百分比点，`20` 表示吃到玩家 `MeleeAttack` 的 `20%`。 |
| `rangedAttackUsagePercent` | 百分比点，`20` 表示吃到玩家 `RangedAttack` 的 `20%`。 |
| `magicAttackUsagePercent` | 百分比点，`20` 表示吃到玩家 `MagicAttack` 的 `20%`。 |
| `summonAttackUsagePercent` | 百分比点，`20` 表示吃到玩家 `SummonAttack` 的 `20%`。 |

### 8.3 首版设计建议

- 武器等级提升应主要提升稳定输出，不应每级同时大幅提升攻击、攻速、范围和暴击。
- 高射速武器每点攻击力价值更高，应降低单发攻击成长。
- 高射速武器尤其会放大固定攻击点；通用输出成长改用 `Damage` 百分比后，仍要检查最终伤害、攻速、投射物、暴击的组合上限。
- 高爆发武器可以拥有更高攻击，但攻速、命中窗口或后摇要承担代价。
- 武器自身暴击率和暴击伤害应保守，避免与角色、升级卡叠加后早期达到高暴击覆盖。
- 近战武器的 `range` 与攻击序列命中窗口要一起调；不要只靠放大范围修复动画命中问题。

## 9. 升级卡数值规范

### 9.1 稀有度与预算

升级卡以 `100` 预算作为普通主属性档位参考。首版建议：

| 稀有度 | 建议预算 | 投放规则 |
| --- | ---: | --- |
| 普通 | 75 到 115 | 可重复投放，优先单属性；双属性要降低单项强度。 |
| 稀有 | 110 到 160 | 可做双属性、小代价或轻机制；强条件卡可略高。 |
| 史诗 | 160 到 230 | 可以有构筑倾向、强属性组合或小机制；通常限制拾取次数。 |
| 传说 | 230 到 320 | 可以改变构筑入口或战斗规则，但必须有前置、互斥、次数限制或代价。 |

机制效果需要额外预算。以下效果不应只按属性点计算：

- 新增武器、升级武器、增加武器槽。
- 增加投射物数量。
- 强制暴击、无敌、不死兜底。
- 经济雪球、经验雪球、商店折扣。
- 波次开始自动获得强 Buff。

### 9.2 投放限制

- 可重复普通卡应优先保持在普通主属性档附近。
- 高价值机制卡应设置 `maxPickCount = 1` 或明确互斥。
- 改变构筑入口的卡应设置最小波次或前置标签。
- 负面属性可以回收预算，但不能抵消明显破坏平衡的强机制。
- 权重只能降低出现频率，不能替代平衡；低权重强卡仍然会主导单局体验。

### 9.3 当前升级卡池基准

当前生成器中，升级卡基础权重大致为：

| 稀有度 | 当前基础权重范围 |
| --- | ---: |
| 普通 | 68 到 120 |
| 稀有 | 44 到 80 |
| 史诗 | 22 到 40 |
| 传说 | 8 到 10 |

当前升级卡池还使用：

- 前一轮出现过的卡权重乘以 `0.5`。
- 标签相关权重通常按已选择事实 `+0.15` 权重倍率累计。
- 部分卡有最小波次、标签数量、武器类型、互斥和最大拾取次数限制。

这些是投放概率规则，不是强度预算本身。

## 10. 饰品数值规范

饰品是商店和宝箱长期装备项，低品质不应抢升级卡成长节奏。

| 品质 | 建议预算 | 属性与机制要求 |
| --- | ---: | --- |
| 普通 | 35 到 70 | 只给纯属性，优先单属性，不给特殊机制。 |
| 稀有 | 75 到 115 | 以属性为主，可以有少量双属性；特殊机制应非常轻。 |
| 史诗 | 130 到 180 | 可以有明确触发条件或小构筑倾向，但不改变核心玩法结构。 |
| 传说 | 200 到 280 | 可以提供构筑入口或强条件效果，但应有代价、限制或触发门槛。 |

首版限制：

- `ProjectileCount +1`、`WeaponSlotCount +1`、强经济雪球应只出现在传说品质，并带明确代价。
- 传说饰品如果没有触发条件或负面代价，预算应靠近 `200` 到 `240`，不要默认顶到上限。
- 低品质饰品不投放 `DamageReduction`、高额 `Dodge`、高额 `HealthRecoverySpeed` 这类容易改变容错曲线的属性。

## 11. Buff 数值规范

Buff 是短时强度调节，不能直接按永久升级卡预算等价处理。它应同时看持续时间、触发频率、来源成本和是否可叠加。

首版建议：

| Buff 类型 | 推荐范围 |
| --- | --- |
| 常规输出 Buff | 5 到 10 秒，单项 `+15%` 到 `+30%`。 |
| 常规机动 Buff | 5 到 10 秒，移动速度乘区 `+15%` 到 `+35%`。 |
| 回复 Buff | 5 到 10 秒，`HealthRecoverySpeed +1` 到 `+2` 优先。 |
| 吸血 Buff | 5 到 10 秒，`LifeSteal +1%` 到 `+3%` 优先。 |
| 无敌 Buff | 3 到 5 秒优先，不再附带大额正向属性。 |
| 不死兜底 | 每波或长冷却触发，避免和无敌、回血、减伤同时堆叠。 |

硬性建议：

- 无敌、强制不死、强制暴击属于机制级效果，不能再叠高额输出或机动。
- 波次开始自动获得的 Buff 应比主动消耗型 Buff 更保守。
- Buff 配置文案必须由结构化属性和 Feature 描述生成或保持一致，禁止手写与实际效果不符的描述。

## 12. 角色数值规范

角色由共享基础属性和角色额外属性组成。共享基础属性不参与角色差异预算，角色额外属性和特殊 Feature 参与预算。

首版建议：

| 角色类型 | 建议额外预算 |
| --- | ---: |
| 标准角色 | 150 到 250 |
| 明确流派角色 | 220 到 350 |
| 高风险高收益角色 | 300 到 450，但必须有显著短板 |
| 测试/调试角色 | 不进入正式内容目录和角色选择目录 |

机制预算建议：

- `ForceCriticalFeature`、`WeaponSlotCount +1/+2`、低血爆炸、死亡兜底等按高价值机制计入预算。
- 初始高等级武器要计入角色强度，不能只看属性预算。
- 角色差异应有方向，例如输出、机动、生存、经济、武器流派；避免同时全方位增强。
- 如果角色有极端数值，应通过名称、目录或内容目录明确标记为测试角色，不能进入正式 `Game Content Catalog`。

## 13. 敌人数值规范

### 13.1 属性职责

- `DetectionRange`：用于感知、索敌、站位决策、行为切换。
- `AttackRange`：用于攻击实际命中范围、近战圆形检测、伤害区域。
- 敌人基础 `AttackSpeed` 是每秒攻击次数，不是百分比点。
- 敌人临时属性变化应通过 `PropModifierData` 和 `PropertiesManager`，不要在行为脚本中直接改基础属性或绕过属性系统。

### 13.2 攻击节奏

敌人普通攻击间隔：

```text
interval = 1 / (AttackSpeed * attackSpeedBenefitRatio)
```

- 普通攻击槽受 `AttackSpeed` 影响。
- 技能槽不受 `AttackSpeed` 影响，使用技能自身冷却。
- 普通攻击默认直接使用敌人 `Attack` 作为基础伤害，不在敌人 SO 上额外配置通用伤害倍率。

### 13.3 首版设计建议

- 普通敌人以清晰单一职责为主，不要同时拥有高生命、高护甲、高伤害、高移速。
- 精英敌人可以突破单项，但应通过体型、动画、行为或波次标签让玩家提前识别。
- Boss 可以显著突破普通预算，但其数值应和阶段机制、技能冷却、召唤节奏一起设计。
- `KnockbackResistance` 只用于敌人或其他可被击退单位，玩家侧默认不投放。

## 14. 内容池、权重和价格

### 14.1 权重语义

内容池 `baseWeight` 是相对权重，不是概率。最终概率受以下因素影响：

- 同池其他候选项权重。
- 条件是否通过。
- 动态权重规则。
- 是否允许重复结果。
- 最大 roll 次数和最大 pick 次数。
- 互斥项。

### 14.2 权重设计原则

- 强度预算决定“是否合理”，权重决定“出现频率”。
- 不要用极低权重掩盖明显破坏平衡的内容。
- 高稀有内容应同时具备低权重和投放条件。
- 对构筑入口类内容，优先使用最小波次、前置标签和最大拾取次数控制。

### 14.3 价格设计原则

- 商店价格和强度预算应大致同向。
- 传说饰品和武器槽、投射物、经济雪球类内容不能只靠高价格平衡，因为宝箱或奖励可能绕过价格。
- 折扣有全局上限，设计折扣卡时应考虑多个来源叠加后接近 `80%` 上限的体验。

## 15. 当前资产待校准清单

以下不是最终规则，而是基于当前资产和本规范首版预算发现的优先风险：

- `Test.asset` 已进入内容目录，数值是测试级，应从正式角色列表移除或隔离。
- `BasePropGroup.asset` 当前有 `WaveGoldRewardBonus +1000`，语义上非常危险；在消费链路确认前不应作为基础属性。
- 本轮已将玩家侧明确的通用 `Attack Add` 输出词条迁移为 `Damage` 百分比属性；后续新增或改旧内容时仍要按正式投放范围逐项审查，避免重新引入全局固定攻击点。
- `Buff_07_无敌` 同时给无敌和高额移速，建议拆分或削弱。
- `Buff_06_再生` 的 `HealthRecoverySpeed +5` 对 10 秒 Buff 来说偏高。
- `Buff_01_迅捷` 的移动速度和攻击范围乘区组合偏强。
- `glass_cannon`、`blood_pact`、`sniper_stance`、`guardian_oath`、`overloaded_magazine`、`close_quarters` 等升级卡按首版预算偏高，需要结合实际体验逐项校准。
- 多个传说饰品预算接近或超过首版上限，尤其是 `WeaponSlotCount`、`ProjectileCount`、高额移速/闪避组合。
- `Character12`、`Character14`、`Character17` 等角色明显超出标准角色差异预算，需确认是正式角色、Boss 角色还是测试角色。

## 16. 新增或修改数值的流程

1. 先确认属性单位，尤其是百分比点和点数属性。
2. 再用属性价值表估算预算。
3. 判断是否包含机制效果，机制效果单独加预算。
4. 确认投放入口：升级卡、饰品、角色、Buff、敌人、商店、掉落、波次。
5. 设置限制：稀有度、权重、前置条件、最大拾取次数、互斥、最小波次、价格。
6. 修改代码生成器时，同步修改已存在资产，避免重建前后数值不一致。
7. 修改描述或 UI 展示时，确认百分比点不重复乘 `100`。
8. 跑静态扫描，确认没有旧 ratio 写法和无效 `PropType`。
9. 进入 Unity 编译和实机验证，重点看早期强度、经济雪球、生存容错和构筑入口。

## 17. 静态检查建议

常用检查：

```powershell
rg -n "0\\.\\d+" Assets/ScriptableObjects/Content Assets/Resources/Data -g "*.asset"
rg -n "propType:\\s*28" Assets -g "*.asset"
rg -n "0%~100%|0~1 表示|倍率统一使用 0~1" Assets/Scripts -g "*.cs"
```

注意：小数不一定错误。`AttackRange +0.6`、`HealthRecoverySpeed +0.3`、`Luck +0.25` 这类可能是合法点数配置，应结合属性单位判断。
